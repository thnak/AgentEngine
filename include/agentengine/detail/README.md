# `agentengine::detail`

Private internals shared across `agentengine` subsystems. Not user-facing: nothing here is part of
the public API surface, may change without notice, and must not be relied on from outside the
library, per CONVENTIONS.md's layout note ("private internals — not user-facing").
