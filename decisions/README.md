# Decisions — the ADR record

An **ADR** here is not a memo. It is the durable record of a `design → red-team → prove → judge`
loop: competing designs implemented in real C++23, attacked adversarially, compiled under multiple
compilers, run under sanitizers, and measured — before a judge picks a winner on evidence.

This is the process Quark uses (36 ADRs and counting), and it exists because the alternative —
settling a hot-path or security-critical question by argument — produces designs that read well and
fail under load or attack.

## When an ADR is required

- Any **security-critical** choice: capability representation, isolation boundary, taint mechanism,
  approval binding, secret handling.
- Any **hot-path** choice measured by a 023 budget.
- Any choice where **credible designs disagree** and the disagreement is not resolvable by reading.
- Any **promotion** of an RFC from Draft to Proven — the gate execution *is* the ADR.

Ordinary implementation choices do not need one. An ADR for everything is an ADR for nothing.

## What an ADR must contain

1. **The question**, stated so that it has a wrong answer.
2. **The competing designs**, each steelmanned.
3. **Falsifiable claims** per design — specifically the fast/safe/correct claims, each paired with
   an experiment that would *disprove* it.
4. **The red-team attack** on each design, hardest where it claims to be fast or safe.
5. **The executed evidence**: commands, compilers, sanitizers, measured numbers (percentiles, not
   means), with **positive controls** — a test that cannot fail proves nothing, and for security
   claims this is mandatory (022 §5).
6. **Per-claim verdicts**: CORRECT / WRONG / INCONCLUSIVE, decided by observed output, not argument.
   `INCONCLUSIVE` is an honest verdict and must not be laundered into either of the others.
7. **The decision**, the specs it binds, and the residual risks or deferred gates.

## Naming

`ADR-NNN-<kebab-case-question>.md`, numbered sequentially and permanently. A superseded ADR is
marked Superseded and points forward; it is never deleted, because the reasoning that was wrong is
part of the record.

**That protection is for decisions, not for documents that were never decisions.** A file that turns
out not to meet "What an ADR must contain" — no competing designs, nothing implemented, no judge
picking a winner — is **removed from this directory outright**, and whatever it established moves to
the home the project already has for it: a toolchain or protocol claim to `docs/research/<date>-<topic>.md`
(CLAUDE.md), a reproducer to `tests/experiments/`, an enacted change to a comment at the site it
governs. Git history keeps the text; the index keeps its meaning. Retiring the number rather than
reusing it is enough — a decisions/ directory padded with documents that are not decisions is worth
less than a smaller one that is all signal, and a bug or a bad call gets reworked rather than
enshrined.

## Index

| ADR | Question | Outcome |
|---|---|---|
| [001](ADR-001-shellrunner-grammar-and-dispatch.md) | How does `ShellRunner` parse and dispatch without a name ever resolving to an arbitrary process, and with the parser itself safe against adversarial input? | **Judged.** Design A (recursive-descent → AST → tree-walking eval) accepted, scoped to Windows/MSVC+clang; Design B never implemented (not defeated on evidence). Partial evidence toward 010 §9 G2/G3/G4; G6 (cross-platform) and others remain open. |
| [002](ADR-002-pythonrunner-embedding-and-mediation.md) | How does embedded CPython enforce a closed import allowlist and mediate `open`/`socket`/`subprocess`, robustly rather than as a degrading blocklist? | **Judged.** Meta-path finder mechanism accepted; "closed by construction" narrowed after prove phase found granting numpy+pandas requires ~130 allowlisted names including `ctypes`/`winreg`/`subprocess` (module-name granularity can't distinguish trusted internals from guest code). See OpenQuestions.md for the caller-aware-gating follow-up. **§11 addendum (2026-08-11), Proposed, awaiting Judged**: §6 item 5 (subinterpreter pooling) answered NO on this build — `Py_NewInterpreterFromConfig` itself works, but `import numpy` fails under both the strict PEP 684 config and the legacy shared-GIL config against the installed numpy 2.3.3, and the strict config's failure crashes the process on teardown (`STATUS_HEAP_CORRUPTION`) — a real, reproduced, process-crash-capable hazard, not merely an incompatibility. |
| [003](ADR-003-caller-aware-import-gating.md) | How does the import gate distinguish a granted package's own internal use of a sensitive module from guest code importing the same module directly (OQ-15)? | **Judged.** Host-side dual-registry + C-level frame-stack-walk mechanism accepted, narrowing ADR-002's scope-limitation for `ctypes`/`_ctypes`/`winreg`/`_wmi`/`_winapi`/`subprocess`. Not airtight (gadget-chaining and C-reentrancy questions remain open); one real entry-point gap (`importlib.import_module` on a cache miss) was found and fixed during independent re-verification, underscoring that this class of design has a demonstrated history of missed entry points. Resolves OQ-15 for the measured target. |
| [004](ADR-004-appcontainer-native-jail-windows-backend.md) | Does AppContainer + Job Object satisfy `native-jail`'s Windows contract across pure-Python, native-extension, and CodeAct-authored guest code? | **Judged (2026-08-23; was "Spiked, not Judged" from 2026-08-02).** AppContainer (zero capabilities + `CHILD_PROCESS_RESTRICTED`) confirmed correct for process/network denial and all three CodeAct library tiers against a vendored CPython 3.14. Headline finding: AppContainer's ACL model is **not** a sufficient filesystem boundary — `win.ini`/`hosts` remain readable via Windows' own inherited `ALL (RESTRICTED) APPLICATION PACKAGES` grants regardless of configuration, confirming 008 §1b's choice to make interpreter-level `open()` mediation primary. **§10 addendum (built and measured, same day):** `JobObjectLimits` — memory/active-process limits precise and reliable; `cpu_ms`/`JOB_OBJECT_LIMIT_JOB_TIME` fired in only 3/11 measured runs and must be treated as best-effort, `wall_ms` is the real enforcement point. **§11/§12 (2026-08-23): the missing independent red-team pass, run for real, plus the LPAC decision, both closed.** LPAC vs. regular AppContainer: real execution (`tests/adr004_lpac_decision_spike.cpp`, new) shows LPAC does NOT close the win.ini/hosts finding — stays with regular AppContainer. The red-team pass (a fresh reviewer, no prior context) found and this same pass FIXED: a **BLOCKING** finding, verified by execution — `exec()`'s `CreateProcessW` had no `PROC_THREAD_ATTRIBUTE_HANDLE_LIST`, so with `bInheritHandles=TRUE` a zero-capability AppContainer child could inherit and read ANY other inheritable handle open in the host process, reproduced directly (a probe child read an unrelated host secret through an inherited handle) — falsifying the AC-S1 "no outbound socket" claim whenever the host had any other inheritable handle open (realistic, since host code owns networking in-process). Fixed: `exec()` now uses the same 3-entry `HANDLE_LIST` pattern `create_python_worker()` already had. Also fixed in the same pass: `exec()`'s `hStdInput` was the host's own real stdin with no capability grant (now an explicit empty `NUL` handle); `create_python_worker()` leaked 4 HANDLEs on every failure path after worker-process creation (new `close_worker_handles()` helper, wired into `destroy()` and all 4 failure returns); `create_python_worker()` re-granted the same deployment-fixed ACL paths on every session, growing the shared profile's DACL without bound (new `grant_ro_deduped()`, mirrors `shared_profile()`'s own reuse model). Re-verified: full rebuild, 255/261 passing (the 6 failures are the pre-existing, already-disclosed ADR-081 Slice-2 regression, confirmed unrelated by `git stash`-testing the pre-fix code), the native-jail-specific suite 11/11. |
| [005](ADR-005-capability-bearer-tokens-cross-process.md) | Should a capability that must cross a process boundary be a self-verifying macaroon-style bearer token, or an opaque reference checked against a host-side registry? | **Judged, narrowly.** Resolves OQ-3. Bearer token (`trust/capability_token.hpp`) accepted for `ExpiresAt`/`PathPrefix` caveats — proven attenuation-only and forge-resistant under red-team (bit-flip, field tamper, caveat-strip, reorder, fabricated-parent derivation all rejected; zero ASan findings, UBSan not attempted — no clang toolchain on this machine), no round-trip required by construction. Host-side registry (`trust/capability_registry.hpp`) not rejected — it is the answer for capabilities needing immediate revocation, a gap the token structurally has. Performance claim left **INCONCLUSIVE**: measured local cost favored the registry, confounded by an unoptimized (uncached) BCrypt handle in this pass and by the registry benchmark omitting the real cross-process round-trip Design A avoids by construction. **Residual (noted 2026-08-23, component-role-audit-tracker.md Finding M):** both designs are real and tested, but neither has a production consumer yet — zero non-test `#include` of either header anywhere in `include/`/`src/`, including the exact boundaries their own comments name as the reason to exist (the `remote` sandbox profile, delegated A2A calls, a remote plugin). "Judged" here means the primitives are sound, not that anything is wired to them. |
| [006](ADR-006-agent-spawn-depth-budget-bound.md) | Are depth and budget bounds sufficient to contain `agent.spawn`'s recursion hazard, and how is the bound represented so guest code cannot widen it? | **Judged (partial — depth only).** Partially resolves 026 §9 Q1 / OQ-14. `SpawnBudget` (`trust/spawn_budget.hpp`) — a private-construction, strictly-decrementing in-process value type, not a cryptographic token (agent.spawn never crosses an OS process boundary) — proven exhaustive over 51 starting depths plus two compile-time `static_assert`s blocking out-of-class construction; zero ASan findings. Depth bounds confirmed sufficient against unbounded recursion, **conditional on** the effect-mediation boundary (006 §9 G4) holding, which this ADR assumes rather than re-proves. Cost bounds (wall-clock/token spend per spawn) remain open, tracked against 023. |
| [007](ADR-007-span-level-taint-vs-per-item.md) | Should taint be tracked at sub-string (span) granularity rather than per-`Content`-item? | **Judged — rejected.** Resolves OQ-5. A span-taint prototype confirmed the precision claim (per-item taint really does deny genuinely trusted material sharing an item with tainted material) but red-team found a naive, otherwise-plausible `concat` implementation silently under-taints real danger bytes on the first attempt — a new security-relevant bug class per-item taint structurally cannot have. The concrete precision gap is already closed today by keeping trusted/tainted material as separate `ContentItem`s (017 §3's existing structural-separation idiom), at the same precision, without that risk. Decision: keep per-item taint (003 §2); do not build span-level taint. |
| [008](ADR-008-wasm3-cold-start-vs-wasmtime.md) | Does wasm3's cold-start advantage over Wasmtime, measured on our own workload, clear the 023-budget bar OQ-12 set before it is even a candidate? | **Judged — the gate clears, the runtime decision is deferred.** Resolves OQ-12. Real, symmetric, correctness-checked cold-start measurement (parse/compile+instantiate+call+teardown, trivial exported function, 1000 iterations, four consistent runs): wasm3 p50 ≈ 0.7 µs vs. Wasmtime p50 ≈ 111-128 µs (160-180x), p99 ≈ 0.7-1.0 µs vs. 241-289 µs (250-400x) — a real, structural gap (interpreter vs. JIT compilation), not measurement noise. 008 §1a's rejection of wasm3 as the *primary* runtime (Component Model/WASI 0.3 support) is unaffected and unchanged. Whether to actually build a scoped second runtime behind the same host interface is left as a separate, larger decision this ADR does not make — it did not evaluate 008 §1a's named cost ("two sandbox-escape surfaces and two conformance stories"), and Wasmtime's realistic cached/precompiled deployment shape (which would narrow the gap) was not measured. |
| [009](ADR-009-capability-set-enforcement-mechanism.md) | How should in-process `Capability`/`CapabilitySet` be represented and enforced so that empty-by-default, attenuation-only, and per-invocation revocation are structural properties, not conventions? | **Judged.** Resolves 007 §3's in-process enforcement half (Milestone 2, Phase A). Parameterized `cap::` variant (16 kinds) + checked `attenuate()` (narrowing-only, all-or-nothing) + shared-ticket `BoundCapability`/`revoke()` accepted; a whole-set epoch-counter alternative rejected by design-level reasoning alone (fails 006 §8 G3 — an ordinary call doesn't bump a macro-level epoch, so a stashed handle would survive to the next call). Proven via a positive-control-bearing suite (six independent widening axes rejected, a stashed-copy handle confirmed dead after revocation, an unrelated handle confirmed unaffected) on Windows (MSVC + ASan, zero findings) and Linux (Docker, gcc-14) — 27/27 checks on both. A real `agentengine::ToolCall` naming collision (capability vs. content.hpp's message-content type) was found via a live build failure and fixed by namespacing the per-kind structs under `agentengine::cap`. |
| [010](ADR-010-wasm-component-host-manifest-capability-binding.md) | How does the `wasm` `SandboxBackend` bind a component's manifest-declared capability requirements to real, checked `Capability` grants at load time? | **Judged.** See the ADR for its own question/decision detail (M2 Phase D). |
| [011](ADR-011-first-party-egress-proxy.md) | How does a first-party egress proxy (008 §10 Q3) mediate outbound HTTP for every sandbox profile without a bypassable boundary? | **Judged.** `HostEgressProxy` (`NetEgressBackend` concept) accepted: resolve-once-connect-to-verified-IPv4-literal closes DNS-rebinding by construction; a blocked-range table checked on the binary address is immune to encoding-bypass classes by construction; CRLF-injection gate; byte cap enforced mid-stream; no redirect-following. Plain HTTP and IPv4 only this milestone, both named explicitly. Verified Windows/Linux with G2-style positive controls (`tests/test_net_egress_proxy.cpp`); a real TCP RST/`ECONNRESET` bug was found and fixed in the test double, not the product code. **Addendum (2026-08-19):** the §9 residual "chunked transfer-encoding is not supported" was itself wrong — a chunked, no-`Content-Length` response doesn't time out (every request already sends `Connection: close`), it silently corrupts `resp.body` with raw chunk framing bytes. Fixed with a real `dechunk_body()`, applied AFTER `parse_http_response()` (not inside it — that broke the streaming path's header-only call on the first attempt, caught by the full suite and fixed same day) in the two buffered exchange functions only; chunk extensions/trailers discarded, malformed framing fails closed with `net.protocol_error`; no change to the read loop's own byte-cap-during-the-loop discipline (claim C8). This also surfaced and fixed a second real bug: `protocol/openai` and `protocol/anthropic`'s `chat()` each carried their own now-redundant chunked-decode workaround that, once the transport layer started dechunking first, ran a second time on already-plain JSON and misparsed it — both retired in favor of trusting `resp->body`. Neither provider's streaming path was touched. Full suite 195/195, zero regressions, both rounds. Bug fix against this ADR's own already-decided design, not a new ADR (ADR-062 removal precedent). |
| [012](ADR-012-sandbox-profile-template-parameter-kind.md) | What kind of thing is `SandboxProfile<P>`'s template parameter `P`: an enum value naming a closed set of engine profiles, or any type satisfying `SandboxBackend`? | **Judged.** `P` is any type satisfying the new `SandboxProfileArg` concept — a real `SandboxBackend`, or the resolution selector `Strict` (replacing the independently-broken, undefined `Profile::Strict`) — never an enum value; 008 §2a's extensibility promise made true by construction. A non-conforming `P` fails to compile at the declaration site, proven by a `try_compile()` gate matching the existing 007 §9 G1/G2 pattern, plus a runtime round-trip test. Both RFCs (002 §2/§3, 008 §3) corrected to match. `check_sandbox_profile_availability` is now real for the resolved cases; `Strict`'s resolution against real backend availability, and the separate per-tool backend-compatibility check, remain deferred — both need machinery (an Engine-level registry, a new policy tag) this ADR did not build as a drive-by. |
| [013](ADR-013-https-egress-tls-client.md) | Where does the egress proxy's TLS client come from, and how is the dependency vendored, given 008 §10 Q3 draws the "needs a vetted crypto library" line at TLS specifically? | **Judged.** mbedTLS vendored directly (3.6.7, exact-pinned + checksummed via `FetchContent`, this project's own established discipline — not Quark's laxer vcpkg-floor `QUARK_WITH_MBEDTLS` path), behind `AGENTENGINE_WITH_HTTPS` (default off). `TlsClientSession` provides real system-CA-style validation (a separately-pinned, compiled-in CA bundle) and real hostname verification, unlike Quark's `SecureTransport` (deliberately nulled-out hostname check for cluster mTLS, re-confirmed unsuitable). Platform-native TLS (Windows SChannel / Linux OpenSSL) was considered and rejected per explicit user direction, trading a bundled CA file's maintenance cost for one portable code path. Proven with in-memory-generated test certificates against a real loopback TLS server: valid-cert positive control, untrusted-root/hostname-mismatch/expired-cert rejection all confirmed on Windows and Linux. A real Quark header bug (missing `<atomic>` include, Linux-only) was found and fixed on this project's own side (Quark never patched in-tree). Default (HTTPS-off) build/test posture reconfirmed fully unaffected. |
| [014](ADR-014-worktree-mount-path-canonicalization.md) | How does a real, OS-backed materialization of a worktree mount resolve a guest-supplied relative path so that `..`, absolute redirects, symlink/junction/reparse-point crossing, ADS, `\\?\` prefixes, unicode-normalization tricks, and TOCTOU re-resolution can never name anything outside the mount root? | **Judged.** Design A (lexical canonicalize-then-string-check, reopen the checked string later) rejected: a real, deterministically-reproduced TOCTOU proof shows the reopened handle reads content that was swapped in AFTER the check passed. Design B (`open_within_mount_root`, `core/worktree_mount_fs.hpp`) accepted: a single handle-based `CreateFileW` lets Windows resolve reparse points transparently, and containment is verified from the resulting HANDLE (`GetFinalPathNameByHandleW`), never from a re-parsed string — proven immune to the identical interleaving (an already-open handle is unaffected by a later swap; a fresh post-swap request re-resolves and is correctly rejected). A real production bug (missing `FILE_SHARE_DELETE` on the target handle, which would let an open guest handle block a host-side delete/replace) was found and fixed during the same prove pass. 22/22 checks, Windows only — 8.3 short-name aliasing and real-symlink-vs-junction parity remain named, untested residuals on Windows. **Addendum (2026-08-06, Phase C4):** Linux parity closed as an ordinary follow-on task (no second ADR) — `open()` + `readlink("/proc/self/fd/N")` (`core/worktree_mount_fs_posix.hpp`/`.cpp`) ports the identical "verify from the object opened, not a re-parsed string" property; 21/21 checks on real, unprivileged Linux filesystem I/O, including the same TOCTOU proof via real symlinks. |
| [015](ADR-015-shellrunner-grammar-parser-fuzzing.md) | How does `MediatedShellRunner`'s grammar parser get continuous, corpus-driven fuzzing under ASan/UBSan, gated in CI, given 010 §9 G8 requires it (and ADR-001's own fixed-corpus proof stopped short of it)? | **Judged.** A real libFuzzer harness (`tests/fuzz/mediated_shell_parser_fuzz.cpp`) against the pure `parse()` entry point, Clang-only (`AGENTENGINE_BUILD_SHELL_FUZZER`, default off), seeded from a checked-in corpus, run 120s/push in a new `windows-shell-fuzz` CI job. Two real Windows libFuzzer ABI mismatches (CRT static/dynamic, MSVC STL ASan container-annotation flag) found and fixed during setup. The harness's teeth were proven directly: ADR-001 finding 12's nesting-depth guard was deliberately reintroduced-as-disabled, produced a real native stack-overflow crash the harness correctly detected via both direct replay and its own corpus-driven crash path, then the fix was restored and reconfirmed clean (23,517 executions, 0 findings). AFL++ (Windows support too weak) and extending the existing fixed-corpus approach (not what G8 asks for) both considered and rejected. Residuals named: the CI job was reviewed but not executed on an actual runner (no configured remote); no semantic oracle (crashes/hangs/UB only, not silent wrong-output bugs); no corpus persistence across CI runs; Linux and the full dispatch/evaluate path both out of scope. |
| [016](ADR-016-provider-egress-address-policy.md) | Should host-initiated provider egress (`perform_provider_https_exchange`) reuse ADR-011's guest-path `resolve_and_validate` blocked-range filter, given the threat model differs (host-authored URLs, not guest-supplied)? | **Accepted** (2026-08-07). Split into `resolve_and_validate(host, port)` (blocked-range filter, WASM guest egress only) and `resolve_host(host, port)` (no filter, host-initiated provider calls) over one shared resolution implementation, so the two cannot drift. Both keep ADR-011's resolve-once-connect-to-a-verified-literal DNS-rebinding defense unchanged. Supersedes the Milestone 5 Phase C implementation note that reused the guest filter "as defense-in-depth even though the threat model differs." |
| [017](ADR-017-stream-consumer-cancellation-signal.md) | How does a stream consumer's cancellation propagate into a provider stream's blocking I/O, closing 004 §7 G2's "no orphaned socket" gate? | **Accepted** (2026-08-07). `make_stream<T>` constructs one `std::stop_source`, copied (not duplicated — shared stop-state) to both the `stream<T>` consumer half and the `stream_producer<T>` half; `cancel()`/destructor request stop, `stream_producer<T>::stop_token()` hands the matching token to `perform_provider_https_exchange`, reaching the mid-flight cancellation Phase C2 already proved at the raw exchange layer. Closes the Milestone 5 Phase J3 residual. |
| [018](ADR-018-agent-session-real-chat-client-wiring.md) | Can `AgentSession<ChatClientT>` host a non-default-constructible real `ChatClient` conformer without breaking every existing default-constructible mock conformer? | **Accepted** (2026-08-07). `chat_client_` becomes `std::optional<ChatClientT>`; every pre-existing conformer is bit-for-bit unaffected (still default-constructible, engaged at construction exactly as the value member was); `emplace_chat_client(Args&&...)` constructs a non-default-constructible client in place after the actor exists. Closes the Milestone 5 Phase J1 residual. |
| [019](ADR-019-incremental-streaming-response.md) | How does a provider response stream incrementally (SSE/chunked-transfer) into `chat_stream()` without baking one vendor's event dialect into shared framing? | **Accepted** (2026-08-07). Three independently-testable layers: stateful pure `ChunkedBodyDecoder`/`SseEventFramer` framing decoders (`sandbox/incremental_http_body.hpp`) that return raw event blocks, not parsed fields, so OpenAI's `data:`-only and Anthropic's `data:`+`event:` dialects both read the same blocks; a streaming transport pair (`perform_http/https_exchange_streaming`) identical to the buffering pair (same byte-cap, no-redirect, `stop_token` cancellation) except body bytes reach an `on_body` sink as they arrive; real Phase D/E backend `chat_stream()` bodies wired to both. Closes the Milestone 5 residual naming this gap in both backends' file banners. |
| [020](ADR-020-portable-reasoning-effort.md) | Should `reasoning_effort` be a portable, backend-independent knob on `ChatRequest`, and is `prompt_cache_key` ready to design alongside it? | **Accepted** (2026-08-07). `enum class reasoning_effort {off, low, medium, high}` as `std::optional<reasoning_effort>`, appended last on `ChatRequest` (003 §6's field-ordering lesson). Chosen over a per-backend constructor field because live-probe evidence showed a real, measurable effect on one backend and none on the other — an agent author needing backend-specific knowledge to "reason harder" was judged worse than the asymmetry. `prompt_cache_key` stays deferred (evidence insufficient to design it yet). Amends 004-Model-Provider-Plane.md §2. |
| [021](ADR-021-inbound-protocol-trust-boundary.md) | Does AgentEngine's inbound MCP/A2A surface terminate TLS+OAuth-2.1-style auth itself (first-party), or defer to an operator-supplied reverse proxy trusting a forwarded, signed principal header? | **Superseded by [039](ADR-039-inbound-transport-host-pluggable.md)'s strategic direction; §7's bearer-token mechanism survives unchanged.** Historical text kept below. Was: **Judged, strategic question only.** Design A (first-party) accepted; Design B (proxy-delegated) not rejected but found to need a structural fix (per-request signed binding) before it's safe, after red-team surfaced HTTP request-smuggling as a principal-forgery/cross-tenant-leak primitive specific to its pooled-connection trust model — a severe gap the original claims table didn't cover. Design C (extend Quark's `TcpTransport`) was too-hastily rejected in the original design pass; Quark's own sanctioned `event_loop()` extension point (already used by `VoiceChannel`/ADR-030) reopens a real "share the reactor, don't touch the wire protocol" option for whichever design's listener gets built. Design A's core cryptographic mechanism is fully proven: `trust/hmac.hpp` (extracted, shared HMAC-SHA256/constant-time-compare primitive, also now used by ADR-005's own capability token) and `trust/bearer_token.hpp` (mint/verify with algorithm pinned by construction — no alg/kid field exists to confuse — audience/issuer validated against caller-supplied config never token content, and a bounded, `exp`-pruned replay guard with its single-instance limitation honestly demonstrated rather than hidden). 32/32 checks including a mandatory positive control. The network listener, server-role TLS, and HTTP/1.1 request-parsing/smuggling-hardening remain real, named follow-on work (each plausibly its own ADR) — Milestone 7 Phase C4+/D5+ stay blocked on that, not on this ADR's own now-settled question. |
| [022](ADR-022-inbound-listener-reactor-model.md) | Does the future inbound HTTP listener (ADR-021 follow-on) share Quark's own `TcpTransport::event_loop()` reactor, or run a fully standalone one? | **Superseded in full by [039](ADR-039-inbound-transport-host-pluggable.md)** — no first-party listener means no reactor question; also moot since ADR-037 deleted the Quark types both designs were built from. Historical text kept below. Was: **Judged.** Design B (standalone reactor) accepted over Design A (shared reactor) and Design C (hybrid accept-then-handoff, rejected outright). Red-team found the shared-reactor starvation risk is not speculative — `voice_channel.hpp`'s own header cites a measured, already-executed negative control (ADR-030's F2r) proving an unbudgeted consumer fully starves the shared loop's other fds at P>=64; this ADR reuses that evidence directly rather than re-measuring it, the same "cite proven evidence" move ADR-021 made reusing ADR-005's HMAC primitive. Design A remains implementable but only under a binding, VoiceChannel-shaped budget discipline that must never regress, with a strictly worse failure MODE for the same class of implementer bug (a loop-thread-contract violation in Design A can corrupt the whole node's cluster-transport state; the equivalent bug in Design B is contained to the HTTP reactor's own maps) — plus a real, previously-unflagged Windows-vs-Linux `add_fd` asymmetry (silent overwrite on Windows, loud rejection on Linux) that breaks any double-registration-guard assumption on this project's primary dev platform. Design B's own claimed isolation was narrowed after red-team found it doesn't cover fd-table exhaustion, heap pressure, or CPU-scheduler contention — real residuals, named, with this project's existing Job-Object-limit tooling (ADR-004) flagged as the applicable mitigation when the listener is actually built. A pre-existing, out-of-this-ADR's-scope gap was found and recorded rather than lost: Quark's own `TcpTransport::on_accept_ready()` accepts any TCP connection with zero authentication before registering it on the shared reactor, meaning the starvation attack this ADR analyzes for HTTP traffic is already possible against Quark's own cluster port today, gated only by network topology. No new measured benchmark code was written — the mechanism in question is unchanged from `VoiceChannel`'s already-measured context, so re-measuring it would reproduce a known result rather than test anything new. |
| [023](ADR-023-response-format-codec-seam.md) | How does AgentEngine extract reasoning/tool-call content from a raw, non-JSON-field model response format (Harmony, DeepSeek/Hermes/Qwen delimiters, `<think>` tags) extensibly, without a text-derived reconstruction gaining a vendor-structured call's unconditional authority? | **Judged (2026-08-10, both phases implemented).** A declarative grammar table (`static constexpr` POD, no regex/code) proven, by independently-reverified execution, to correctly extract tool calls from real Harmony/DeepSeek/Hermes/Qwen example strings. Red-team found the naive "scan only at stream boundaries" mitigation fails against a laundering attack (a model quoting attacker-controlled tool-result text that contains valid call syntax) and that schema-only auto-declassification is unconditionally unsafe (invisible to existing taint-based policy); accepted instead a capability-scoped declassifier. **Shipped, both halves:** (1) `include/agentengine/core/response_format_codec.hpp`, wired into `OpenAIChatClient` behind an off-by-default `scan_response_format_leaks` flag — Reasoning/`<think>`-channel extraction for UI. (2) A real `007 §4` amendment (`call_provenance`) plus `invoke_tool` step 5 rewired (`core/tool_pipeline.hpp`) so a `text_derived` call auto-declassifies only when the target tool's capability ceiling is provably inert (`trust::is_inert_for_text_derived_declassification`, an exhaustive 15-kind allowlist) and declared `pure`; otherwise mandatory approval, unconditionally — overriding even a tool's own `Approval<never_require>`. The confused-deputy scenario the red-team found is now a permanent regression test (`tests/test_tool_pipeline.cpp`'s `ADR-023 P2-T2`). 150/150 tests green, zero regressions. **Not shipped** (named residuals, §7): format coverage beyond the 4 built-in rows, the `ae:codec` WASM plugin path, streaming decode. |
| [026](ADR-026-milestone-status-doc-accuracy-and-drift-lint.md) | Are `CLAUDE.md`, `README.md`, and the marketing site's milestone-status claims consistent with the real, landed code — and is any drift between them structurally caught, or only found by accident? | **Judged.** Found (via the same audit, independently re-verified) that all three independently claimed "Milestones 7-9 have not started" or worse; the marketing site's copy additionally folded in the already-complete Milestone 6 and claimed zero implementation exists for MCP/A2A/AG-UI, contradicted by real, tested headers. Corrected all three to a three-state description (complete / in progress, exit criterion not yet met / not started). Shipped `tools/milestone_status_lint.py`, wired into CI with no `continue-on-error`, sentence-scoped (not paragraph-scoped) specifically to avoid a self-collision bug found during design review — the naive version would have falsely re-flagged the corrected Milestone-7 sentence because of the adjacent, unrelated Milestone-8/9 clause. Proven both directions before merge: a positive control (run against the original stale text) actually fails with 5 findings; the corrected text passes clean. Named, not fixed here: `apiContent.ts`'s deeper per-protocol notes and RFC status table carry the same class of staleness at a larger scope than this change verified. |
| [025](ADR-025-naming-lint-namespace-scope-and-027-vocabulary-diagram.md) | Was 027's G1 naming-lint gate actually checking every exported name in `agentengine`'s public core, or only a subset the scanner happened to see? | **Judged, scanner fix only — bulk reconciliation explicitly deferred.** Found (via a full-codebase audit, then independently re-verified) that `tools/naming_lint.py` matched the exact string `"agentengine"` only, silently skipping any C++17 nested-namespace module — 24 headers across `trust/`(7), `sandbox/`(4), `workflow/`(9), and `core/`'s `schema`/`json`/`yaml`/`response_format_codec`(4), ~70+ declarations, entirely unaudited by a real, required CI gate with no `continue-on-error`. Included 027 §4's own flagship worked examples (`Executor`/`Edge`/`Workflow`/`EdgeFailurePolicy` in `graph.hpp`) and the capability-token/bearer-secret public surface (`CapabilityToken`, `SecretKey`, `HmacSha256`). Fixed to recognize `agentengine::<module>` as in-scope except `::detail` (and anything nested under it); wire-protocol namespaces stay excluded via the pre-existing directory-level filter, unaffected. Measured before/after on the real tree: 76 unlisted + 112 suppressed (188) before, 136 unlisted + 127 suppressed (263) after — both a real `exit 1`, the gate still honestly failing, now against the complete surface. 027 §6's namespace diagram corrected to list the real layout. Deliberately does **not** attempt the ~263-name bulk reconciliation against 027 §2-4 in this same change — red-team found that fixing the scanner and reconciling in one pass risked landing a technically-larger-but-still-incomplete table update; sequencing (fix and prove the gate first, triage separately) is recorded as the decision, not the reconciliation itself. |
| [024](ADR-024-skill-scoped-tool-and-mount-wiring.md) | Should a skill's `allowed-tools` additionally gate which tools are declared/invocable (not just which content is readable), and how does a mounted skill's content become reachable by a real sandboxed tool call at all? | **Judged, with a same-day addendum.** Surveyed MAF/Anthropic/OpenAI: none vary tool declaration by loaded-resource state, all scope only content. User directed a deliberate departure anyway — `core/skill_tool_scoping.hpp` computes one `ToolTable` per run, used for BOTH declaration and every `invoke_tool(...)` call (the real enforcement boundary is `invoke_tool`'s own step-1 `table.find()` against the CALLER's table, not `ContextContribution.tools` — filtering only the latter would be cosmetic). A real, independent bug was found and fixed while wiring the other half: `SkillsProvider`'s `Mount.mount_id` was a path-shaped string that could never have matched native_jail's single-path-segment mount-id convention; renamed to the bare skill name. `native_jail::materialize_skill_mounts` (new) bridges `SkillsProvider::mounted()` into real host directories via the previously-uncalled `materialize_mount`. **Addendum (same day):** narrowed further from "every resolved skill, unconditionally" to agent-triggered on demand — a new always-available `mount_skill(name)` tool, backed by `MountedSkillsState` (real, persistent, per-run state, not a chat-history message the way MAF's own stateless/advisory `load_skill` is — verified directly in MAF's C#/Python source that `read_skill_resource`/`run_skill_script` never actually check `load_skill` ran first). Mounting grants no new authority (I3) — every resolved skill's files are already readable unconditionally; mounting only activates already-pre-authorized tool declarations. Negative-control-proven offline (a real tool rejected by `invoke_tool` when unmounted, succeeding after) and run live end to end against a real OpenRouter model twice — once reading a mounted skill's content via real mediated `open()`, once self-sequencing `mount_skill` before `execute_code` purely because the tool wasn't declared yet. Residuals: no type-level guarantee ties the declared/invocable tables together (convention only); only `native_jail`'s Python runner is wired, not `MediatedShellRunner`/`wasm`; `AgentSession` still owns no sandbox/tool-call loop in production, so `MountedSkillsState` sharing is a process-scoped-static workaround, not a general mechanism. |
| [028](ADR-028-session-scoped-stateful-tools.md) | Does closing "`Tool<>::invoke()` has no path to `AgentSession`-owned state" (ADR-024 §8's named residual) require a new type-erasure mechanism, or can it stay inside this codebase's existing conventions? | **Judged.** First draft (extend `ContextProvider` with an optional `StateT`-aware `on_context` overload) rejected by red-team on the strongest grounds: it solves a problem that doesn't need solving — `HistoryProviderT history_provider_` is already a per-`AgentSession`-instance member, so a state-aware provider can already own its own session-scoped state as ordinary members, exactly the pattern `SkillsProvider`/`HistoryAndSkillsProvider` already establish; zero core-seam change needed. Confirmed exhaustively this session: this codebase has never used `std::any`/`std::type_index`/`typeid`/`dynamic_cast` anywhere (RTTI avoided on principle, both AgentEngine and vendored Quark) — ruling out a type-erased `void*` slot as foreign to convention. Accepted design: `make_tool_descriptor_with_invoke<ToolT>(InvokeFn)` (`tool_pipeline.hpp`), preserving `ToolT`'s compile-time-checked `Capabilities<...>`/`Approval<...>`/`EffectClass<...>` extraction while letting a provider's own member function supply the actual invoke logic. Red-team found one **fatal** hazard, fixed structurally not by convention: a state-capturing closure marked `Backgroundable` is a real dangling-reference/data-race hazard (`start_background_task()` already detaches a real thread with no synchronization against `fork_from()`/`clear_in_process_state()`, neither of which is `protocol`-serialized) — closed via a new `ToolDescriptor::captures_session_state` flag checked and refused in `background_task()`'s own authorize step, before step 8. Proven: state persists correctly across rounds within one run; two independent sessions never see each other's state (would produce a wrong, shared total if they did); the Backgroundable-conflict combination is refused with a specific error code, no thread ever detached; the existing `make_tool_descriptor<ToolT>()` path is completely unaffected. 179/180 tests pass (same pre-existing, unrelated failure ADR-024/027 already recorded — corrected 2026-08-11: two real, deterministic test-authoring bugs, not a flake; see ADR-024 §6). Explicitly not claimed: real CodeAct wiring (`MediatedPythonRunner`/`MountedSkillsState`) is separate, later work; the interpreter's own one-per-process constraint (a scoped ADR-002 security-mechanism decision, not a CPython law — subinterpreters are a real, still-open candidate per ADR-002 §6 item 5) is untouched; "provider-owned state must never hold a capability" is a real, type-system-unenforceable invariant, named with the same prominence ADR-024 gives its own unenforced invariants. **Same-day addendum:** `fork_from()`/`clear_in_process_state()` now copy/reset `history_provider_` too (a real gap this ADR's own §6 first only named — re-examining the prior code showed the old "leave it untouched" choice was correct before this ADR, wrong after; a fork's counter now correctly continues from the source's copied value, and a deleted session's provider state leaves no residue), proven in the same test file's S5. |
| [027](ADR-027-agent-session-tool-call-loop.md) | Should `AgentSession::handle()`'s tool-call loop live inside the actor's own coroutine (one `StartRun` → N internal rounds → one final response), or stay external, formalized into a reusable helper instead of three drifting hand-rolled copies? | **Judged.** User directed "inside," matching `turn_index`'s own pre-existing intended-shape comment. Red-team found one **fatal** issue in the first draft: `ToolCall::provenance` wasn't threaded into `ToolCallRequest::provenance`, silently defaulting every call to `vendor_structured` and reopening the exact confused-deputy gap ADR-023 §4b Finding 1 closed (a bug that already existed, independently, in all three external-loop copies this ADR replaces — never previously exercised unattended). Fixed once, in a new shared `tool_call_extraction.hpp`, and regression-verified both directions during design (the reintroduced bug was confirmed to make the new test's provenance assertion fail; the fix was confirmed to make it pass again). Also confirmed (not merely assumed) that the two live e2e tests would break and `cli_chat.cpp`'s external loop would go dead the moment this shipped — all three rewritten in the same change. `ToolTable::from_descriptors(contribution->tools)` rebuilt fresh every round from the same snapshot used to declare tools that round closes, for this loop, the "declared ≠ invocable" gap ADR-024 §3a/§7 named as convention-only. 178/179 tests pass (the one failure, `test_mediated_python_runner_hostile_corpus`, is the same pre-existing, unrelated failure ADR-024 §6 already recorded — corrected 2026-08-11: two real, deterministic test-authoring bugs, not a flake). Residuals named, not fixed: synchronous tool calls block the Quark worker thread (native tools must stay non-blocking); approval-gated tools with no configured decider are indistinguishable from ordinary non-convergence; null `capabilities_` denies per-call rather than aborting the run; `on_turn_end`'s per-round `TurnView` no longer includes the run's input message; `AgentMetadata::max_turns` still isn't wired to the new runtime `max_turns_`; no mid-loop checkpointing. Session-scoped stateful tools (CodeAct/skills) and suspend-for-human approval are explicitly out of scope, decided before design started, not discovered gaps. |
| [031](ADR-031-spawn-cost-budget-actor-primitive.md) | Does routing a consumable spawn-token pool's check-and-decrement through a real Quark actor's own `Sequential` dispatch actually close the concurrent double-spend 026 §9 Q1's own already-red-teamed sketch predicts a bare copyable value type (like `SpawnBudget`'s depth-ceiling shape) would suffer? | **Judged.** Not a fresh design — 026 §9 Q1 already contains a full design → red-team cycle (2026-08-11) explicitly marked "no code, not an ADR"; this ADR turns that sketch into real, tested code. `SpawnCostBudgetActor` (`trust/spawn_cost_budget.hpp`): a real `quark::Actor<..., quark::Sequential>` holding one consumable `std::uint64_t` pool, deliberately a SEPARATE type from `SpawnBudget` (depth is a ceiling, safely re-derivable by concurrent siblings from the same parent value; tokens are a consumed pool, where two concurrent spawns each attenuating a bare copy could each succeed past the true remaining amount). Check-and-decrement happens as one step inside the actor's own Sequential handler, closing the race by construction via Quark's own already-relied-upon dispatch guarantee, no hand-written lock. One deliberate deviation from `AgentSession`'s own "never respond on failure" idiom, explained not silently copied: that idiom is safe only under `TestKit`'s synchronous resolution — `reply_cell.hpp`'s own comment confirms an unanswered `Ask` under a REAL engine only resolves at actor teardown, which would hang a live caller — so this actor always responds, using `result<SpawnTokenGrant>`. Proven under a REAL, multi-worker `quark::Engine` (not `TestKit`, which cannot produce a genuine race): 8 concurrent `std::thread` callers contending 130 tokens each against a 1000-token pool (guaranteed contention, not a lucky fit) never grant more than 1000 total across 5 consecutive re-runs, with the actor's final `remaining()` exactly reconciling against the sum granted every time. Not claimed: `agent.spawn` itself still has zero real call path anywhere in this codebase (re-confirmed by the same exhaustive survey ADR-030 independently ran) — no nested-agent-run invocation mechanism, no sub-worktree wiring, no production `AgentSession` tool-call loop to host a spawned child; this ADR proves the standalone cost-pool primitive only, matching ADR-006's own precedent for the depth half. No refill/top-up mechanism (deliberate); no attribution/audit trail; wall-clock/deadline budgeting stays fully open. |
| [032](ADR-032-workflow-executor-worktree-scoping.md) | Can a per-workflow-executor `sharing_mode` policy be decided and turned into a real, capability-gated worktree grant using ONLY 025 §3's already-shipped `SubWorktree`/`Mount` primitives, closing 014 §1's named "nothing states whether a workflow executor gets its own sub-worktree, inherits the caller's, or shares one" gap? | **Judged.** Independent red-team (via the `Agent` tool) on the FIRST draft found one fatal flaw (a `readonly` grant cannot actually be mounted — `Mount`/`mount_read` has no pinned-digest read path, only a `Ref`-backed one) and six must-fix issues (resume would silently re-branch and clobber in-progress work; the suggested `run_id()`-derived precondition is temporally impossible; unsanitized executor ids splice into worktree/mount paths; `WorkflowBuilder`'s C++ authoring surface had no way to actually opt an executor into `shared`; `branch`'s whole-parent-tree initial content was undocumented and untested; `graph.hpp` would have swallowed `worktree.hpp`'s full actor/persistence/capability dependency graph for one enum) — all seven fixed before the accepted design shipped, not dropped as residuals. Accepted design: `Executor::worktree_mode` (`workflow/graph.hpp`) defaults every executor to `branch` UNCONDITIONALLY (not 025 §3's "sequential defaults to shared" applied per-node — `WorkflowSupervisor`'s superstep model means any two executors reachable in the same round get concurrent asks, and general graphs here have cycles and dynamic routing that make proving two nodes structurally can't co-occur unsound to attempt), with `shared`/`readonly`/`scratch` reachable as an explicit per-executor opt-in via `TypedExecutor`'s own field. `workflow/worktree_scoping.hpp`'s `mint_executor_worktrees`/`resume_executor_worktrees` turn the declaration into a real `SubWorktree` + capability-gated `Mount`, fail closed on a double-mint (`worktree_scoping.already_minted`) rather than silently re-branching over existing work, and fail closed rather than fabricate on an unreconstructible resume (`readonly`'s pinned digest lives nowhere durable — `worktree_scoping.readonly_resume_unsupported`). Proven directly against `mount_read`/`mount_write` (not through a live `FunctionExecutor` — no production host builds one yet), 11 test blocks / 47 checks, each tracing to a specific red-team finding rather than a generic happy-path sweep, including a simulated-restart resume proof (M8) and a pre-existing-content-visible-through-a-fresh-branch proof (M5) closing the "proof plan would pass regardless" complaint the red-team raised about the first draft. Not claimed: no wiring into a running `FunctionExecutor`'s `EffectContext`; `agent`/`sub_workflow`-kind executors untouched (not built); merge-on-join unbuilt; `readonly` executors have no guest-facing `Mount` at all (needs a `core/worktree.hpp` primitive change, out of scope). |
| [033](ADR-033-middleware-model-call-chain.md) | Can 002 §5's model-call `Middleware<Ms...>` interception point be turned into real, composed code using the same decorator-wrapper shape `ResilientChatClient<Inner>` already established, while genuinely satisfying "may not widen capabilities"? | **Judged.** Independent red-team found ONE FATAL, exploitable flaw: a `before_model`/`after_model` hook can freely rewrite `ChatResponse.message.content`, and a `ToolCall` defaults to the FULLY TRUSTED `call_provenance::vendor_structured` — `invoke_tool`'s step 5 checks that only against the target tool's own `approval_mode` (may be `never_require`). A middleware fabricating or mutating a `ToolCall` therefore launders an untrusted decision through the exact channel a genuine vendor-returned call travels — the confused-deputy shape ADR-023 already closed once for raw model text, relocated to a middleware content rewrite. Closed by `enforce_backend_tool_call_provenance`: any `ToolCall` in the final response not byte-identical to one the REAL backend actually returned is forced to `call_provenance::text_derived` before the wrapper ever returns it, routing it through ADR-023's stricter gate (which overrides even `never_require` for anything with a real capability ceiling or non-pure effect class) — proven both for fabrication (a genuinely clean backend response gets an appended forged call) and for mutation of an already-genuine call's arguments, with a mandatory positive control proving an untouched genuine call keeps its trust (the mechanism is targeted, not a blanket downgrade). Three further must-fix gaps closed: §5's own "attributed to it by name in the trace" constraint had no wiring at all (fixed: every hook-bearing middleware must declare a compile-time `name`, an optional caller-injected trace hook fires structured attribution events, mirroring `WorkflowSupervisor::CheckpointHook`'s established idiom); composition order with `ResilientChatClient` was unspecified despite the two orders NOT being interchangeable (documented as a mandatory contract — `MiddlewareChatClient` must be the OUTER layer, never the inner, or retries would re-run the whole before-phase under one "stable" idempotency key and a fallback-content hook could silently corrupt circuit-breaker health tracking); hook-exception semantics were undefined (fixed: a throwing hook is caught and converted to a `result<T>` failure, never escapes as a raw exception, matching every other control-flow surface in this codebase). Also independently confirmed and fixed: 002 §5's own snippet wrote the hook return type as `ae::task<>`, which `quark/core/task.hpp` confirms has no co_await protocol at all and is unbuildable as written — corrected to `ae::task<std::monostate>` (RFC text updated to match, per CLAUDE.md's "fix the spec first" rule). `MiddlewareChatClient<Inner, Ms...>` (`core/middleware.hpp`) is the accepted mechanism: a `ChatClient`-conforming template decorator (same shape as `ResilientChatClient<Inner>`, never a virtual base), first-registered-outermost ordering, forward before-phase/backward after-phase onion unwind, `ModelCallContext` deliberately carrying no `EffectContext&`/capability type at all (I2 enforced by omission, not a runtime check). 15 test blocks / 30 checks, all CORRECT. Scoped to the model-call interception point only — run/turn/tool-call points and `AgentSession` wiring are explicitly not attempted, matching ADR-028's own "prove the mechanism against one real consumer" precedent; streaming (`chat_stream`) is forwarded uninterceped. **Amendment (2026-08-12):** `MiddlewareChatClient` itself was REMOVED the same day as `FailoverChatClient`/`ResilientChatClient` (this repo had shipped nowhere, so no deprecation-then-migration cost) — `MiddlewareModelCallGateway<Inner, Ms...>` (ADR-036) is the real consumer of this ADR's own machinery (`ModelCallContext`/`run_before`/`run_after`/`enforce_backend_tool_call_provenance`, all reused verbatim) now; its regression suite (T1-T15) survives, ported to the new wrapper in `tests/test_middleware_model_call_gateway.cpp`. |
| [030](ADR-030-session-scoped-codeact-wiring.md) | Can ADR-028's `make_tool_descriptor_with_invoke` mechanism give CodeAct real per-session state, and which of `cli_chat.cpp`'s five process-global statics (`MediatedPythonRunner`, `ExecState`, `MountedSkillsState`, mount roots, a duplicate `SkillsProvider<>`) can actually become per-session vs. which must stay process-wide? | **Judged (2026-08-13).** First draft (each session's provider owns its own `MediatedPythonRunner`) rejected by red-team on two fatal, independently-confirmed grounds: `MediatedPythonRunner::run()`/`refresh_agent_tools()` route through unsynchronized, process-wide globals (`g_current_ctx`, `config_.tool_bridge`), so two sessions each owning a runner and calling it concurrently is a real cross-session confused-deputy leak, not just a crash; and cwd/env/the work-mount directory are OS-process-global, so a genuinely per-session `ExecState` can't isolate two sessions through one interpreter regardless of ownership (CPython's classic embedding API supports exactly one `Py_InitializeFromConfig` per process). Accepted: the runner stays a process-wide singleton, reached through a new `CodeActRunnerBinding<RunnerT>` that makes ADR-002 §5.5.6's "one interpreter per process" rule structurally unbreakable (first session to `bind()` claims it for the process's life; any other session_id fails closed) rather than merely commented — closing the confused-deputy hazard by making it impossible for a second session to ever reach the runner at all. What DOES become genuinely per-session: `MountedSkillsState`/`ExecState`, now real `ToolDeclaringHistoryProvider` members reached via `make_tool_descriptor_with_invoke`, safe specifically because the claim guarantees at most one session is ever live against the shared runner. A new `AgentSession::history_provider()` accessor lets `main()` configure the provider post-construction (the seam ADR-028 §6 left unbuilt). Proven: the claim primitive itself (first-caller-wins, idempotent same-session re-bind, fails closed for a different one) and, at full-stack integration (two real `AgentSession`s, a real tool-call round loop), that `MountedSkillsState` genuinely never leaks between sessions and the claim is enforced at the point of USE, not just at configure time (an unconfigured/rejected session is denied on every attempt to reach the shared runner). `tools/cli_chat.cpp` itself rebuilds and links clean against the corrected design. Not claimed: true multi-tenant CodeAct (two sessions BOTH executing Python) is still impossible — this ADR makes the second session's rejection safe, not the scenario possible; `fork_from()`'s interaction with a bound runner claim is a named, unexercised residual (a forked session inherits a pointer to a binding still bound to the source's id, and nothing re-validates it against the fork's own identity — `cli_chat.cpp` never forks, so unexercised in production); the shared "work" mount directory isn't forked either, matching ADR-028's own precedent for that class of gap. |
| [029](ADR-029-suspend-for-human-approval.md) | Can a round genuinely suspend mid-tool-loop for a real human's approval answer — resumable from a separate ask, continuing the same run/turn — without overflowing Quark's fixed message-pool cap or duplicating the whole round-loop body for the resume path? | **Judged (2026-08-13).** First draft (widen `StartRun` with an optional resuming-interaction-id field) rejected by red-team on a real compiled measurement: `sizeof(quark::Ask<StartRun, AgentResponse>)` would grow from 160 to 200 bytes, over Quark's hard 192-byte `MessagePool::kMaxPayload`. Accepted: a separate `ResolveInteraction` message (measured 136 bytes) plus factoring the round loop into a shared, genuinely-awaitable `run_rounds<AskT>()` (`quark::task<std::monostate>`, per ADR-047 — a bare `task<>` cannot be `co_await`ed by a sibling handler) that both `StartRun` and `ResolveInteraction` drive. Red-team found and closed six issues before acceptance, the sharpest being: validate a resume against `history_`'s actual pending-call tail BEFORE resolving the `Interaction` (not after, which could resolve against a round nobody actually reviewed); reject a fresh `StartRun` while a round is suspended (would otherwise interleave a second concurrent run); admission-check `ResolveInteraction::caller` the same way `StartRun::caller` already is (an unrelated principal must not resolve someone else's pending approval by guessing an id); a resume continues the SAME `run_id`/`turn_index`, never a fresh run (I4/I8). Proven in `tests/test_agent_session_suspend_approval.cpp` (27/27 assertions, deterministic/offline): suspension fires before any call invokes; a second `StartRun` while suspended never resolves; `approved=true` invokes the exact pending call for real and converges; `approved=false` feeds back a denial without ever invoking the tool; an unknown interaction id fails closed touching nothing; a mismatched caller is denied at admission before the lookup runs. Opt-in only (`suspend_for_approval_`, default false) — with it off, or with a real `ApprovalDecider` configured, behavior is unchanged from ADR-027. Not claimed: `Interaction::expires_at_ns` remains unwired (a suspended run with no human answer waits forever); the suspended round's own pending message isn't durably checkpointed yet (the same `Message`/`ContentItem` serialization gap ADR-027/028 already name); no partial (per-call, sub-round) approval; no real approval UI/transport — session-actor mechanism only. |
| [034](ADR-034-agentsession-streaming-turn-loop.md) | Can `AgentSession::handle()`'s internal loop stream each round's model call for real, live token-by-token rendering in `cli_chat.cpp`, without breaking failover/circuit-breaker-feedback/response-format-leak-scanning for every OTHER caller, and without letting 004 §5's `TokenBudget<N>` silently stop being enforced? | **Judged.** First draft (`chat_stream()` as `handle()`'s ONLY model-call path, unconditional) rejected by red-team on three independent fatal grounds, each read directly from the real backend source: `FailoverChatClient::chat_stream()` never falls over to a secondary backend (a named, deliberate scoping decision in that file); `ResilientChatClient::chat_stream()` never reports an outcome to its own circuit breaker; and `OpenAIChatClient`'s ADR-023 confused-deputy leak-scan (`apply_response_format_scan`) runs only from `chat()`, never `chat_stream()` — an unconditional streaming core loop would silently drop all three for every caller composing them under `AgentSession<ChatClientT, ...>`, not just the CLI. Accepted: streaming is a per-session opt-in (`set_stream_model_calls(bool)`, default false, same shape as `suspend_for_approval_`) — every existing/default caller keeps calling `chat()` bit-for-bit (re-proven by re-running `test_agent_session_tool_call_loop.cpp`'s full R1-R6 unmodified); an opted-in session accepts losing the three protections above for itself, made visible (not silent) via a `run_event_kind::warning` fired once per streaming run. A second, independently real gap closed: `ChatResponseUpdate` had no usage field at all, so streaming with no safeguard would let `TokenBudget<N>` silently stop being enforced — fixed by adding `std::optional<Usage> usage` to `ChatResponseUpdate` (additive), wiring `OpenAIChatClient`'s streaming path to request/parse OpenRouter's trailing `stream_options.include_usage` chunk, and making `AgentSession`'s new `run_model_call()` helper fail the run CLOSED (`run.usage_unavailable`, ask never resolves) whenever a stream completes with no usage — never silently zero-cost. Message reconstruction from accumulated deltas needs no merge step: `tool_call_extraction.hpp`'s `tool_calls_of()`/`text_of()` are genuinely position/count-agnostic across however many content items a Message carries (verified, not assumed). Proven in `tests/test_agent_session_streaming_model_calls.cpp` (S1-S4, 16 checks, a real `chat_stream()`-backed scripted ChatClientT built on `core/stream.hpp`'s real `make_stream<T>`): streamed no-tool-call responses reconstruct correctly with real `model_delta` events firing per delta; a streamed multi-round tool-call conversation converges correctly through the unmodified invocation pipeline; a stream with no terminal usage fails closed; the accessor pair defaults false and round-trips. `tools/cli_chat.cpp` migrated off `quark::TestKit` (documented as fully synchronous, "no threads, no wall-clock" — genuinely incapable of hosting concurrent live printing) onto a real single-worker `quark::Engine`, the same real-Engine-hosting-an-AgentSession shape `test_agent_session_suspend_resume.cpp` already established; a per-turn `std::jthread` drain thread prints `model_delta` text as it arrives with RAII-guaranteed `request_stop()`+`join()` on every exit path. Verified live against a real OpenRouter session (`api-test.txt`): streamed turns completed with real usage captured (no `run.usage_unavailable`), tool pipeline and skill mounting unaffected. Not claimed: `AnthropicChatClient`'s streaming path is not wired for usage capture this pass (fails closed if used, does not silently miscount); the leak-scan gap is named, not closed, for the streaming path itself (only made visible via the warning event); `cli_chat.cpp`'s own Engine-hosting shape has no dedicated regression test of its own, resting on the cited precedent plus the live run. Full suite: 206/206 pass. **Amendment (2026-08-12):** `FailoverChatClient`/`ResilientChatClient`, cited above for their `chat_stream()` scoping limitations, were REMOVED that day — ADR-036's `ModelCallGateway` is the real, streaming-capable successor with retry/failover/breaker feedback all genuinely working, closing the gap this red-team finding only worked around. |
| [035](ADR-035-chatclient-streaming-completeness.md) | Can every `ChatClient` conformer become genuinely streaming-capable — closing ADR-034's two named residuals (Anthropic usage, the leak-scan gap) and converting every dead `chat_stream()` stub to real streaming — ending with `chat()` no longer required by the concept at all, without silently weakening any protection or losing coverage? | **Judged.** Four phases, each independently built/tested/committed. Phase 1a: `AnthropicChatClient`'s `StreamingUpdateAccumulator` now wires its own already-tested `message_start`/`message_delta` usage-reduce functions to actually populate `ChatResponseUpdate::usage` (previously computed but never attached) — closing the gap that would otherwise fail every Anthropic streaming call closed once `chat()` is gone. Phase 1b: the ADR-023 leak-scan, previously reachable only through `OpenAIChatClient::chat()`, relocated to a shared `core/response_format_leak_scan.hpp` and applied uniformly by `AgentSession::run_model_call()` regardless of backend or streaming — an independent red-team pass found a real double-scan laundering hazard in the naive version (a second pass could re-decode a first pass's own diagnostic text and promote a different, attacker-chosen candidate) closed by skipping already-`tainted` items, proven via a dedicated regression test (L4) rather than mere absence-of-failure. Phase 2 (giving the three `chat()`-only wrapper templates real `chat_stream()` parity) was attempted and abandoned after red-team found a genuine, unresolved use-after-free hazard specific to middleware hooks under a non-coroutine `chat_stream()` — see ADR-036, this ADR's own superseding replacement for that piece. Phase 3: a full-codebase survey found 46 files with dead, type-satisfying-only `chat_stream()` stubs (never exercised by any test) plus two production call sites (`MemoryProvider`/`HistoryProvider<Summarize<...>>`) still calling `chat()` directly; all 46 converted mechanically (five parallel `Agent` batches, same prescriptive pattern, zero test assertions touched, 208/208), both production call sites moved to a new shared `core/chat_stream_drain.hpp` helper (also backfilled into `ModelCallGateway`'s own duplicate copy, a pure refactor reverified against its own suite), and only then — with every conformer independently proven capable — `chat()` dropped from `ChatClient`'s `requires` clause (widening what can satisfy the concept, deleting nothing, 208/208 unchanged before and after). Not claimed: `chat()` itself still exists on every real conformer and on the three legacy wrapper templates (unmodified, their formal fate a separate future decision); `quark::error`'s coarser failure vocabulary remains a real, named limitation versus `chat()`'s own richer classification; no new live-network run was added beyond what ADR-034 already established. **Amendment (2026-08-12):** the "separate future decision" on the three legacy wrapper templates was made the same day — `FailoverChatClient`/`ResilientChatClient`/`MiddlewareChatClient` REMOVED (unshipped anywhere, no deprecation cost), superseded by ADR-036's `ModelCallGateway`/`MiddlewareModelCallGateway`. A code-review pass on this ADR's own diff also found and fixed two real gaps: `chat_stream_drain.hpp`'s `drained_failure_to_agent_error()` hardcoded `failure_class::transient` regardless of the underlying error (real I2 hazard — a permanent denial could be retried forever by a `failure_class`-keyed consumer), and `AgentSession::run_model_call()`'s non-gateway branch still unconditionally required `chat()` to compile, making the concept relaxation fictional for its one real consumer — both fixed, see the ADR document's own §7a. |
| [036](ADR-036-model-call-gateway.md) | Once the naive plan to give `FailoverChatClient`/`ResilientChatClient`/`MiddlewareChatClient` real `chat_stream()` parity hit a confirmed, unresolved use-after-free hazard (a middleware hook coroutine driven through a non-coroutine `chat_stream()` via a "resume once and hope" trick can be resumed later by whatever it awaited, touching long-gone stack state), what actually delivers retry/failover/middleware under streaming safely? | **Judged.** Two small, separately-testable types instead of one that owns everything (a C++ class template can have only one trailing parameter pack, so `Fallback...` and `Ms...` can't share a template anyway, and the project owner separately directed against folding this into `AgentSession`, which already owns the turn loop/tools/approval/budgets/history/leak-scan): `ModelCallGateway<Primary, Fallback...>` (retry + one real `quark::CircuitBreaker` per backend + failover, reusing `RetryPolicy`/`BreakerConfig` verbatim) and `MiddlewareModelCallGateway<Inner, Ms...>` (middleware hooks over any `ModelCallGatewayLike` `Inner`, reusing ADR-033's `ModelCallContext`/`run_before`/`run_after`/`enforce_backend_tool_call_provenance` verbatim). Both expose exactly one method, `call(request, ctx) -> task<result<ChatResponse>>` — a REAL coroutine, so a middleware hook's `co_await` is completely ordinary, structurally closing the use-after-free hazard rather than defending against it at runtime. `AgentSession<ChatClientT, ...>`'s own template signature is unchanged (62 existing call sites unaffected) — its `requires` clause widened to accept either `ChatClient` or the new `ModelCallGatewayLike` concept, dispatched via `if constexpr` in `run_model_call()`; a gateway-routed round trades live `model_delta` events for the reliability guarantee, named via a one-time warning event matching ADR-034's own pattern. A second, independent red-team pass on the drafted design (before implementation) found and closed three further concrete issues: the enclosing class's own `requires ChatClient<ChatClientT>` clause needed to change too, or the new branch would be dead code; a naively default-constructed `std::array<CircuitBreaker,N>` would silently ship non-functional (zero-cooldown) breakers; and the required `OpenAIChatClient::chat_stream()` HTTP-status co-fix (needed so retry decisions are meaningful against the real backend, not just test fixtures) must classify by status code only, never reuse the non-streaming path's dynamic body-derived message, since `quark::error::detail` is a non-owning `string_view` fed from a detached background thread's soon-dead stack. Proven: `tests/test_model_call_gateway.cpp`, 20 checks — retry, non-retryable-fails-fast-then-fails-over, a tripped breaker sheds admission with an exact call-count proof (not timing-inferred), the middleware fatal-finding fix (fabricated `ToolCall` forced to `text_derived`) reachable through the new composition path, and full `AgentSession` integration (zero live deltas, the warning fires). Full suite 208/208, both before and after a later refactor shared the gateway's internal drain loop with ADR-035's memory/history provider work. Not claimed: the three original wrapper templates are unmodified, not deprecated, not migrated off; `quark::errc`'s retry classification stays structurally coarser than `chat()`'s own; no live-network verification specific to this ADR (rests on the real backends' own separately-verified `chat_stream()`); the last-attempt-could-stream-live optimization was considered and deliberately not built. **Amendment (2026-08-12):** the three original wrapper templates (`FailoverChatClient`/`ResilientChatClient`/`MiddlewareChatClient`) were REMOVED the same day — unshipped anywhere, so no deprecation-then-migration cost; `RetryPolicy`/`BreakerConfig`/`real_jitter` (still shared with this ADR's own types) moved from the now-deleted `resilient_chat_client.hpp` to a new `retry_policy.hpp`. A code-review pass also named (not fixed) a real residual: `attempt_with_retry`'s backoff blocks the calling actor-worker thread (`sleep_for`, no `co_await` across the retry loop) — pre-existing, `ResilientChatClient::chat()` had the identical pattern; a real fix needs a coroutine-suspending timer primitive that does not exist anywhere in Quark's `task<>` today. |
| [038](ADR-038-rt-project-passivation-semantics.md) | Once Quark's actor engine (and its `.passivate()`/lazy-reactivation mechanism) is removed, is there anything real left behind 030 §4's "Pause" verb, or does it reduce to a bare status flip? | **Judged.** What survives: passivation always did two things — evict from memory, and flush durable state first. Removing the actor engine removes the former completely (a plain `rt::AgentSession` has no runtime managing its residency); the latter survives as `rt::ProjectSupervisor::checkpoint_members_and_workflows()`, and `rt::pause_project()`/`archive_project()` flip the manifest's status ONLY if every checkpoint succeeds (a real, deliberate fail-closed choice the Quark original's bare-`bool` `passivate()` had no shape to make). 030 §7 G1's own scale-isolation gate is reproven (`test_rt_project_scale_isolation.cpp`, N=100) but its latency-regression half is deliberately NOT reproven — `rt::` land has no shared scheduler/lock left for `pause_project` to accidentally contend on, so a timing check here could never fail; the structural isolation check (pausing #50 touches only store #50) stands in for it, proving the hazard class can't arise rather than "wasn't observed." Not claimed: no eviction/reactivation exists to reprove (the Quark original's own Dormant-census claim has no `rt::` equivalent, named not dropped); no sandbox-count claim (matching the Quark original's own scope limit); no retention/GC for `archive_project()`. `project/lifecycle.hpp`/`project/project.hpp` (the old Quark originals) deleted outright, zero remaining real consumers once their two test files were superseded. |
| [039](ADR-039-inbound-transport-host-pluggable.md) | Should AgentEngine build a first-party TLS+HTTP listener for MCP/A2A (ADR-021/022's direction), or expose only transport-agnostic protocol handlers and let the embedding application (or a reverse proxy) supply the actual socket/TLS layer, matching MAF's `MapMcp()`-on-Kestrel and the OpenAI Agents SDK's uvicorn/Starlette model? | **Judged.** Supersedes ADR-021 §3-8 and ADR-022 in full. Reopened because ADR-037 made ADR-022's "which reactor" question moot (its Quark types are gone) and because this project's own dispatchers (`McpServer::dispatch`, `A2aServer`, AG-UI's `to_sse_frame`) were already transport-agnostic in-process functions — ADR-021 recorded that fact but never followed it to "so let the host supply the transport," building a first-party listener anyway. Accepted: no first-party listener, ever, for v1 — a host-pluggable `InboundTransportRequest`/`InboundTransportResponse` boundary is the target shape (not yet plumbed into the real dispatchers — real follow-on work, not claimed done). Independent red-team found two FATAL issues in the first draft: (1) the draft assumed a per-call `Principal` that doesn't exist on the real `dispatch()`/`send_message()` signatures; (2) the naive fix — construct a fresh dispatcher per HTTP request — collides with `McpServer`'s own documented "must outlive every task it starts" contract (a backgrounded `tasks/call` completion firing into freed memory), a lifetime hazard structurally identical to ADR-021's own request-smuggling-as-forgery finding, just relocated from a pooled connection to a short-lived object. Fixed by design correction, not by claiming the plumbing exists: one dispatcher instance per AUTHENTICATED SESSION (never per-request), held via shared ownership for at least as long as its connection lives, as a binding contract on every host-pluggable transport adapter. A third MUST-FIX (no bridge from verified bearer-token claims to a `Principal`, so every adapter would hand-roll its own inconsistently) was closed with real code this pass: `trust::principal_from_bearer_claims()` (`trust/bearer_token.hpp`), proven in `tests/test_principal_from_bearer_claims.cpp` (9/9 checks) — sub/tenant_id map correctly, `kind` is an explicit non-token-derived override, no other claim leaks through, distinct subjects never collide. Two residuals named, not closed: `A2aServer` has zero principal/tenant scoping today (`a2a/server.hpp`'s own comment already says so), a real gap the moment more than one `Principal` can reach one instance; and a reference/example transport adapter under `examples/` (not core) risks the same "example silently becomes de facto production" drift this project already found once (ADR-026) unless M7's conformance suites are built to run against any conforming adapter, not a hardcoded one — a design constraint named for the harness, not yet built. ADR-021's own real, Judged bearer-token/HMAC mechanism is NOT superseded — it survives unchanged as a reusable primitive a host adapter may call. |
| [037](ADR-037-remove-quark-as-core-runtime.md) | Should AgentEngine stop building its session/workflow/tool runtime on Quark's actor engine and become a self-contained C++ SDK (MAF/OpenAI SDK/Anthropic-shaped), with Quark reduced to an optional deployment target rather than the foundation? | **Judged (2026-08-14).** Phases 1-5 (§7) were EXECUTED (2026-08-13): every `quark::` production dependency was ported to `agentengine::rt::`/vendored standalone and the `third_party/quark` submodule itself was removed from the tree, re-verified via a from-scratch build. The one residual its own completion note flagged at the time (a stale vendored CA-bundle hash pin) was resolved the same day, commit `3962fa2`, 174/174 green — no longer open as of sign-off. Two independent audits (`Explore` agents reading real source, not memory) grounded the question before any design was proposed: distribution/cluster machinery has ZERO footprint in AgentEngine's current build (no includes, no CMake flags, no compiled object code — Quark's own `CMakeLists.txt` compiles only `src/version.cpp`; the cluster/net headers are unused template surface) and is explicitly named in AgentEngine's own RFCs as deferred future work, not accidental baggage — so the stated objection ("distributed components clutter the implementation") does not describe anything currently in the build. What IS deep: `AgentSession`/`WorkflowSupervisor`/`FunctionExecutor` are literal `quark::Actor` CRTP instances (not callers of Quark), the 192-byte mailbox cap shapes message design, `core/stream.hpp`'s own backpressure scheme is shaped around a Quark ring-buffer limitation, persistence goes through `quark::EventLog`/`FenceToken`, and ~42/208 test files drive `quark::TestKit`/`quark::Engine` directly. Proposed target architecture (§4): AgentEngine owns a small `ae::task<T>` coroutine type (no longer a `quark::task<T>` alias); `AgentSession` becomes a plain class with I1 ("one session, one executor") re-derived via an internal in-flight guard instead of actor-mailbox exclusivity; a small, purpose-built thread-pool executor drives I/O continuations (no mailbox, no supervision DSL, no distributed anything); `stream<T>`'s public shape is unchanged, only its backend swaps; persistence becomes a host-injected `SessionStore` interface rather than a runtime-owned mechanism; `WorkflowSupervisor`/`FunctionExecutor` get a hand-rolled supervised-retry loop; `CircuitBreaker` gets reimplemented (small, already self-contained); the test harness gets a small AgentEngine-owned synchronous driver. Red-team findings (self-authored; independent pass recommended before Phase 1): I1 moves from structurally-enforced to runtime-checked, a real regression in safety-by-construction that needs its own enforcement discipline; this invalidates the PROOF ARTIFACTS behind 15+ Judged ADRs (027-036) even though their BEHAVIORAL claims should survive — re-authoring ~42+ test files against the new substrate is likely the single largest cost in this whole effort; the new executor is unproven infrastructure exactly where Quark currently provides a mature, independently-red-teamed scheduler for free — the single largest NEW risk this ADR introduces; persistence's hardest problem (safe concurrent snapshot) is being reimplemented, not relocated, and needs its own design pass. Proposed phasing (§7): Phase 1 builds the new substrate standalone (Quark still runs everything real); Phase 2 migrates `AgentSession`, re-proving each existing ADR's behavioral claim one at a time; Phase 3 migrates workflow; Phase 4 removes the Quark submodule; Phase 5 re-verifies the full suite and retires `AgentEngineSpecification.md`'s "Quark mapping" section. Not claimed: no code exists; the threading-model default (synchronous vs. thread-pool-async) is flagged, not decided; no timeline commitment; Quark's other locked decisions (WASM ABI, embedded CPython, no microvm, CRTP + declarative authoring) are orthogonal and assumed unaffected. **Addendum (2026-08-19):** the executed Phase 1 red-team's own predicted risk ("the new executor is unproven infrastructure... the single largest NEW risk this ADR introduces") materialized for real, found via red-team against decisions/ADR-064-recall-tool-sync-invoke-vs-async-embedder.md's design review: `rt::ThreadPool::run_job()`'s original `while (!done()) resume();` loop was genuine UB (a cross-thread coroutine double-resume) the moment a `ThreadPool`-driven job's `AsyncMutex`/`channel<T>` await genuinely contended — never actually triggered in this codebase (every real job today, including `AgentSession` via `tools/cli_chat.cpp`, happens to complete in exactly one `resume()`), but nothing prevented it. Fixed same day: `run_job()` now resumes exactly once and fails the job loudly (a diagnosable `JobOutcome::faulted`) instead of looping if it isn't `done()` yet — safe by construction, not merely by luck of today's deployment shape, reusing `AsyncMutex`/`channel<T>`'s own already-proven "destroyed while parked = safe cancel" contract (ADR-017). New `tests/test_rt_thread_pool.cpp` T6 proves the fault fires correctly AND that the mutex is unharmed afterward for later legitimate use. Full suite 194/194 (`ctest -LE live-network`), zero regressions. |
| [052](ADR-052-shell-registered-tool-dispatch-scope-correction.md) | Does this codebase have a shell command dispatcher that invokes a registered `Tool` while bypassing 006 §3's real capability/approval/provenance pipeline, reachable from real agent execution? | **Judged.** The bypass CODE is real (2026-08-10 gap audit #13, re-verified) — `shell_dispatch.cpp`'s `dispatch_command()` calls `resolved.tool->invoke(argv, ctx)` directly, no gates at all — but it has never been reachable: `command_registry.hpp`'s own pre-existing `RegisteredTool` comment already states this is "the minimum type-erased shape needed to make CommandRegistry's three-way lookup real and testable... NOT an implementation of 006's ten-step pipeline," and `shell_dispatch.cpp` (this session's own earlier ADR-056 already confirmed by name) has zero production call sites — only two test files include it. The REAL, live native-jail shell path (`mediated_shell_dispatch.cpp`) has zero "tool"-related code at all, confirmed by direct search — it doesn't dispatch registered Tools by name today, so there is no bypass to close there either. The audit's other named symptoms (a null-`tool_bridge` crash, collapsed capability/approval denials, no real `call_index` source) don't exist anywhere in this file — they describe requirements for a real integration that has never been built, not a fix to existing code. No functional change: a warning comment lands at the exact `resolved.tool->invoke()` call site and on `RegisteredTool`'s own definition, both stating that any REAL future Tool-dispatch integration (spike or production) must route through `invoke_tool()`, never this closure directly — closing the actual risk (a future maintainer copying the anti-pattern) without inventing unscoped feature work this gap never asked for. `test_shell_runner_proof.cpp` re-verified passing unchanged. Full suite green. |
| [051](ADR-051-workflow-scheduling-shuffle-test.md) | Does 014 §8 G3's own scheduling-shuffle test (shuffling intra-round executor scheduling across 10³ seeds produces identical output; an order-dependent executor is caught) exist anywhere in this codebase? | **Judged.** No (2026-08-10 gap audit #22, re-verified) — confirmed directly, no file matched "shuffle" in the workflow-scheduling sense before this ADR; the M6 breakdown doc's own decisions 5/8 only ever DESIGNED this test. Found and fixed a real, narrower documentation gap along the way: that same breakdown doc's own Phase J completion section silently dropped G3 between "delivered" (only 014 §8 G1/030 §7 G1 claimed) and "explicitly deferred" (G3 absent from that list too) — corrected in the same pass with an honest "Update" note rather than silently rewritten. Built `test_rt_workflow_supervisor_scheduling_shuffle.cpp`, reusing `test_rt_workflow_supervisor_patterns.cpp`'s own FI-1/FI-2 fan-out/fan-in shape (which already proved a narrower, single-hand-picked-delay version of this exact claim) broadened to a real 1000-seed sweep of independently randomized per-branch delays, proven against the CURRENT (post-ADR-037) `rt::ThreadPool`-backed fan-out mechanism, not the historical, now-deleted Quark one the original design prose describes. Self-red-team: a positive-only proof would be vacuous without a real negative control, so a second graph with a deliberately order-dependent executor (leaking its own real completion position into its output via a shared atomic counter) is run across a 200-seed subset and shown to actually diverge — proving the positive result is a real check, not something that would pass regardless. One `WorkflowSupervisor`/`ThreadPool` built once, re-run sequentially across all seeds (never spawned per-seed, matching decision 8's own machine-safety framing). Full suite green (~19s added, an accepted, previously-anticipated cost). |
| [050](ADR-050-usage-usagedetails-canonical-name-reconciliation.md) | Which of 027's vocabulary table and the shipped `Usage` struct is the one that actually drifted? | **Judged.** The CODE, per the audit's own framing (2026-08-10 gap #9) — but re-reading 003 §6 and 004 directly (not the audit's paraphrase) shows both already, consistently, normatively spell it `Usage`, never `UsageDetails`; 027's own table row was the one that disagreed with its own cited source. `tools/naming_lint.py` confirmed this structurally: `Usage` needed an `ae-naming-lint: allow` suppression to avoid being flagged unlisted, exactly the signature a genuinely-drifted name would carry — except the "spec" it drifted from was itself the outlier. `ADR-025` had already explicitly deferred this exact question to its own ADR rather than address it inline. Fixed via one table-row correction (027 §2: `UsageDetails` → `Usage`, marked **ours** with an honest divergence rationale per 027 §1's own required discipline) plus removing the now-unnecessary suppression comment on `core/content.hpp`'s `struct Usage` — no code renamed, since the code was already right. Verified with a real before/after naming-lint run: 96 → 95 suppressed findings, the SAME 202 unlisted names (proving this didn't silently absorb any of ADR-025's own deliberately-deferred bulk-reconciliation backlog). |
| [049](ADR-049-cross-provider-reasoning-exclusion.md) | Does a `Reasoning` content item ever get excluded from a turn's assembled context when it was produced by a DIFFERENT backend than the one currently bound (003 §8 Q2)? | **Judged.** No such check existed anywhere (2026-08-10 gap audit #20, re-verified) — `Reasoning` carried no producer field at all, so the comparison Q2 already resolved (2026-08-04: "included only when it originated from the ChatClientId currently bound... exclusion recorded in the trace") was structurally unimplementable. The audit's own cited blocker (`FailoverChatClient`'s ambiguous identity) is now moot — that type was removed (ADR-036 amendment); the audit's own "real production call site was missed" note is correct in spirit but names the wrong file — `assemble_context()`'s `ContextDrop` mechanism is unused by `AgentSession` at all (confirmed directly against `context_assembly.hpp`'s own top comment), the real site is `run_rounds()`'s direct call to `history_provider_.on_context()`. Fixed: `Reasoning` gains `producer_chat_client_id` (appended last), stamped by `AnthropicChatClient` (the only real producer in this codebase — confirmed OpenAI's backend has no reasoning-trace field on a response message at all) at its own single choke point (`StreamingUpdateAccumulator::release()`), consumed via a new optional `HasProducerChatClientId` concept (ADR-044's `has_agent_description` precedent) so a `ChatClientT` without it gets no filtering rather than a compile error. Self-red-team: threading the agent's compile-time `ChatClientId<...>` policy tag through was considered and rejected in favor of deriving identity from the bound backend's own real construction state (more correct, no concept-breaking plumbing); filtering runs at content-ITEM granularity, not whole-message (proven: a message with both a cross-provider Reasoning item and a Text item keeps the Text, drops only the Reasoning); an empty/unstamped provenance defaults to EXCLUDED (Q2's own allowlist wording), covering `text_derived` leak-scan items and pre-existing records alike. First real producer of `run_event_kind::policy_decision` (013 §1's own long-dormant vocabulary). New test file + Anthropic translation extension, full suite green. |
| [048](ADR-048-outbound-media-capability-gate.md) | When a `ChatRequest`'s history carries `Media` content (image/audio/video/file) the bound backend can't encode, does the caller find out? | **Judged.** No (2026-08-10 gap audit #19, Phase 1 only — Phase 2 real wire encoding stays blocked on RFC 019's blob-store seam, confirmed absent from the tree) — both real backends' outbound `translate_message()` silently drop `Media` unconditionally, regardless of what `ChatClientCapabilities` declares, since neither ever checks the capability bit before not-encoding it. Fixed with `validate_outbound_media_capabilities()` (`core/chat_client.hpp`), wired into `AgentSession::run_model_call()` before any backend branch — a request carrying ungrantable Media now fails closed with a real, attributable error naming the offending message/media_type, and the backend's own `chat()`/`chat_stream()` is never invoked at all. Recurses into `ToolResult::content` too, closing the gap's own named "Media nested inside ToolResult::content... unaddressed by either phase" residual as a direct, low-cost consequence of writing the walk correctly. Scoped strictly to the four `multimodal_in_*` bits — proven that a Reasoning item never trips this gate, and `Citation`/`Custom` are correctly out of scope (003 §1's Citation-as-annotation resolution; Custom has no capability bit at all). Two new test files (pure-function + end-to-end through a real AgentSession run), full suite green. |
| [047](ADR-047-memory-ranking-product-formula.md) | Does `rank_memory_items()` compute 029 §5's own literal ranking formula, a product of salience, recency, and tag/keyword overlap? | **Judged.** No (2026-08-10 gap audit #18, re-verified) — the formula was additive (`salience + keyword_overlap_score`) and never used recency at all; the audit's own first-pass alternative (a full commit-history scan per item) doesn't work against this codebase's actual worktree model (a `Ref` carries no parent/history chain, confirmed directly). Fixed with a real recency signal instead: `MemoryItem::write_seq`, stamped from the memory ref's own append-log `SeqNo` (`rt::AppendLogStore::last_seq`, ADR-037) at write time — O(1), no history scan. The formula is a genuine product of three non-degenerate factors, not the raw signals directly: self-red-team found (before any test ran) that a literal product over raw values is actively broken two ways this codebase genuinely hits — `keyword_overlap_score` is 0 on the ordinary turn (a raw multiplicative 0 would zero out every item's score on most turns), and `MemoryProvider::on_turn_end()` never sets `salience` on extracted items (a raw multiplicative salience factor would make every such item permanently rank-zero). Both got small positive floors instead. Recency is normalized against the current retrieval batch's own maximum `write_seq`, bounded to `[1,2]` — an unnormalized raw SeqNo would eventually let mere recency dominate the whole product regardless of relevance, also caught before testing. New test file (`test_memory_ranking_formula.cpp`) proves recency participates, is bounded (an old-but-far-more-salient item still beats a barely-relevant newer one), a zero-salience item isn't structurally rank-zero, plus determinism and a legacy-record (`write_seq==0`) no-division-by-zero control. Every pre-existing ranking-outcome assertion in `test_memory_provider.cpp`/`test_memory_retrieval_determinism.cpp` re-verified unchanged under the new formula; `examples/08_memory.cpp`'s exact-content-match assertion corrected alongside ADR-046 (same pass). Full suite green. Floor/normalization constants are named as reasoned engineering defaults, not evidence-tuned. |
| [046](ADR-046-memory-confidence-labels-and-system-message-separator.md) | Can the model (or a human) tell a `UserStated` memory fact from a `ModelInferred` guess once it's injected into a turn? | **Judged.** No (2026-08-10 gap audit #17, re-verified) — every `MemoryItem` rendered as bare `item.content` regardless of `origin.source`, even though 029 §3 calls `MemorySource` "a trust signal, not decoration." Re-grounding found the audit's own recommended approach named a real, adjacent bug worth fixing first: `protocol/anthropic/chat_client.hpp`'s `split_system_messages()` concatenated every system message's text with ZERO separator (confirmed directly — `out.system_text += t->text`, no boundary at all; its own pre-existing test only "worked" because its fixture happened to supply a leading space), which would have made a bare label prefix bleed into whatever text followed it. Fixed both: a real `"\n\n"` separator between concatenated system-text fragments (never as a leading/trailing pad, so a lone message's own text is unchanged byte-for-byte), and a confidence label (`⟦memory:user-stated, high confidence⟧` etc.) prepended to every rendered memory item, in BOTH places one reaches an agent as text — default injection AND the `recall(query)` tool's reply, not just one. The label is derived only from the trusted, structured `origin.source` field, never from content. Self-red-team named the adjacent forgery surface the audit itself flagged: `on_turn_end()`'s `ModelInferred` extraction is itself model-generated text, so an item's own content could embed literal label text to impersonate a higher-trust item once concatenated — closed by `neutralize_forged_memory_labels()`, which breaks any literal occurrence of the marker's open token inside retrieved content with a zero-width space before a real label is ever prepended, proven directly (the real marker appears exactly once across two items' rendered text, the genuine one's own label, never twice, even when the hostile item's content contains the exact marker string). Does not touch `ContentItem::tainted`/`origin` (029 §6's actual enforcement, unchanged, still proven by `test_memory_no_authority_laundering.cpp`) and does not address gap #21's broader raw-`ContentItem::text` claim. Full suite green. |
| [045](ADR-045-json-schema-validator-subset.md) | Does this codebase have any way to check a JSON instance against an arbitrary JSON Schema document, closing 006 §8 G2 and 015 §7 G2? | **Judged.** No (2026-08-10 gap audit #3, re-verified) — only schema-generation-from-C++-type machinery existed. Fixed with a deliberately scoped subset validator (`type`/`enum`/`const`/`required`/`properties`/`additionalProperties`/`items`/length+range keywords/local `$ref`), NOT the full 2020-12 vocabulary — `pattern` (regex) is deliberately never implemented, structurally avoiding the whole ReDoS risk category rather than trying to bound one (the codebase has zero `std::regex` usage anywhere, confirmed, so this isn't fixing an existing bug, it's not creating one). Real, reachable value scoped honestly: native tools already have stronger type-directed checking via `schema::from_json<Args>()`; this validator's actual consumer is the non-native (WASM/MCP/A2A) tool-invocation path gap #4's registry design targets — not built yet, so no production call site exists to wire this to today. `validate_schema_well_formed()` does real DFS $ref-cycle detection (015 §7 G2, path-scoped not globally-visited, so a legitimate DAG-shaped schema isn't falsely flagged); `validate_instance()` needs no separate cycle check since a finite instance structurally bounds it — proven directly, not just asserted. Self-red-team caught two real bugs before/via testing: `max_violations` alone doesn't bound total work for a huge-but-valid instance (added `max_nodes_visited`), and a latch-ordering bug where the "budget exceeded" flag silently blocked the violation reporting it (found by the test failing, fixed by reordering). Also fixed a real UB risk (an int64_t round-trip on adversarial numeric input) before it ever reached a test. 22-check new test file, full suite green. |
| [044](ADR-044-agent-workflow-description-version.md) | Do `AgentMetadata`/`Workflow` carry the `description`/`version` identity fields 002 §1 has always specified? | **Judged.** No (2026-08-10 gap audit #2, re-verified). Not new surface — 002 §1/§7 named `{agent_id, description, version}` as agent identity from the start; three independent compilers (A2A Agent Card, Agent YAML, Workflow YAML) hit the identical missing-field gap independently, each honestly recording it rather than working around it. Fixed by adding the fields to both structs and wiring the 4 real call sites (not 5 — MCP's tool listing was never a wiring point). `register_agent<A>()` detects the new statics via `requires`-based `has_agent_description`/`has_agent_version` concepts rather than requiring them, avoiding a breaking migration across every existing `Agent<Derived,...>` conformer. Named, not silently ignored: `Tool<Derived,...>`'s own CRTP `description` convention shares the member name with a different meaning — no actual compile collision (unrelated CRTP bases), but a real "could be confused" risk. Checked, not assumed: grepped for positional `AgentMetadata{...}`/`Workflow{...}` aggregate-init call sites before landing fields inserted mid-struct (zero found — every real site is named-field assignment or a fluent builder). I6 test extended (`test_agent_yaml_compiler.cpp`/`test_workflow_yaml_compiler.cpp`), full suite green. Does not close the A2A modality-declaration gap (still no home for `default_input_modes`/`default_output_modes`) or the `Agent`/`Tool` naming overlap beyond flagging it. |
| [043](ADR-043-ack-durability-policy.md) | Does `rt::AgentSession` acknowledge a turn only after its effects are durable, and if not, is there a real opt-in? | **Judged.** No opt-in existed (2026-08-10 gap audit #15, re-verified). The audit's own cited blocker was wrong on both counts: a `SessionStore` seam already exists and is already wired (it was never meant to be type-erased — a `concept`, matching `ChatClient`/`SecretStore` precedent), and 005 §2 already specifies the exact policy vocabulary (`at_most_once_ack`) the audit reinvented names for. Fixed via two new free functions, `start_run_with_ack_policy()`/`resolve_interaction_with_ack_policy()`, that sequence Store writes around the existing `start_run()`/`resolve_interaction()` calls WITHOUT touching `AgentSession` itself — preserving the file's own already-stated "AgentSession has no ambient Store access, I2" boundary. `require_durable` durably writes both a new per-turn `TurnDeltaRecord` (005 §2 says "history delta," not full history — reuses `rt/message_codec.hpp`'s already-proven Message↔JSON codec, making REAL conversation durability tractable now rather than blocked on `AgentSessionRecord`'s separate, larger, not-yet-built full-history gap) and the existing session bookkeeping record, fail-closed on either failing. Surfaced and fixed a real, latent ADL ambiguity in `message_codec.hpp`'s own unqualified internal calls (collided with `chat_recording.hpp`'s separately-maintained same-named functions the moment one test file included both). Self-red-team named a real residual: delta capture is race-free for sequential single-caller usage (005 §2's own G2 gate) but not for concurrent overlapping calls on one session — would need a `run_rounds()`-internal change deliberately avoided here. New test file (T1-T3) proves at_most_once is a store-free no-op, require_durable's delta round-trips byte-correct through JSON, and durability failure fails closed. Full suite green. Does NOT serialize the whole conversation history or wire a restore path — named, not claimed. |
| [042](ADR-042-context-instructions-taint-channel.md) | Does `ContextContribution.instructions` actually reach the model, and is the channel it rides taint-checked? | **Judged.** No, on both counts (2026-08-10 gap audit #16/#21, re-verified against current code). The real drop site is one level later than the audit implied: `assemble_context()` reads `.instructions` correctly, but `rt::AgentSession::run_rounds()` builds `ChatRequest` without ever consulting it, and `ChatRequest` had no instructions slot to put it in. Separately, `Tainted<T>`/`TaintedText` (Judged as a type, 007 §9 G2) was never used as an actual content-model field anywhere. Fixed by retyping `ContextContribution::instructions` to `TaintedText` (forcing every contributing provider to make an explicit trust decision at construction), then wiring the one real declassification site into `run_rounds()`, which synthesizes a `role::system` Message and prepends it. Self-red-team named two real residuals rather than overclaiming: the type system can't stop a careless/bad-faith wrap (matches `Tainted<T>`'s own documented contract everywhere else it's used), and the new channel has no token-budget enforcement. New tests (`test_context_assembly.cpp` B3-I1-I3, new `test_rt_agent_session_instructions.cpp` T1-T3) prove multi-provider concatenation, correct message shape/position, and that the no-instructions path is byte-identical to before this fix. One pre-existing test (`test_memory_retrieval_determinism.cpp`) broke on `Tainted<T>`'s deliberate lack of `operator==` and was fixed to declassify explicitly before comparing. Full suite 176/176 green. Does NOT close gap #21's broader scope (`ContentItem::text` and friends stay raw/ungated at `cli_chat.cpp`/`mcp/server.hpp`/`tool_bridge.hpp`) or wire `AgentMetadata::agent_instructions` (separately-scoped, already-named gap). |
| [041](ADR-041-appcontainer-ace-leak-accepted-residual.md) | Does Windows `native-jail` leak curated host files (`win.ini`, `hosts`) via inherited AppContainer ACEs in a way any per-app fix could close, and is it currently unmitigated? | **Judged.** Real (ADR-004 §5.3/§6.1), but not fixable the way the 2026-08-10 gap audit #11 assumed: its cited blocker, `CreateProcessAsUser`/`CreateProcessWithTokenW` privilege, is used nowhere in this codebase and was never proposed in ADR-004 — the real launch mechanism (`CreateProcessW` + `SECURITY_CAPABILITIES`) already runs under a standard non-admin token per ADR-004 §5. The real, ADR-004-evidenced obstacle is different: writing a DENY ACE onto `win.ini`/`hosts` (both `SYSTEM`/`TrustedInstaller`-owned) would hit the same `WRITE_DAC`-denied failure ADR-004 §5.4 already found for a different case. Root cause is Windows' own default DACL on a curated OS file set, independent of anything this backend grants. Ships no new code: ADR-004 §6.1/008 §1b already specify the accepted fix (interpreter-level `open()` mediation as the PRIMARY filesystem boundary, AppContainer ACL as backstop only) and it is already real, Judged, shipped code (`mediated_python_runner.cpp`'s `Internal_open`, `MediatedFileSystemAdapter`) — a request for `win.ini` never reaches the leaky ACL check because the mount-boundary check rejects it first. The residual only matters for native code bypassing Python-level mediation entirely, which AppContainer's ACL is explicitly a backstop for, not a primary boundary against. Evidence that the leak is real but bounded already exists and already passes: `tests/test_native_jail_abuse_corpus_windows.cpp` Case 4 pairs an arbitrary non-curated secret (`ESCAPE_DENIED`) against `win.ini` (`ESCAPE_OK`, documented) in the same run — this test predates this ADR (`M2 Phase C task C3`, commit `b39f5ea`); ADR-004 §11 item 1 asked for exactly this and was never marked done, a real instance of doc-drift corrected in the same pass. |
| [056](ADR-056-fs-quota-capability-gate-fix.md) | Did every `cap::FsRead`/`cap::FsWrite` check in `mediated_shell_dispatch.cpp`/`mediated_python_runner.cpp` correctly accept a quota-capped grant with headroom and deny a write once real usage already exceeds it? | **Judged.** No, on both counts (2026-08-10 gap audit #12, re-verified against current code). Every FsRead/FsWrite check built a synthetic `nullopt`-ceiling request and checked it via `contains()`, which `cap_covers()` rejects against any quota-capped grant as a widening attempt — a quota-capped grant was unconditionally unusable, the identical bug Milestone 3 Phase G4 already fixed once for the Python write path but never carried to the shell dispatcher (or to either file's FsRead side, which the original audit explicitly deferred but this ADR folds in). Separately, `MediatedFileSystemAdapter::write_file` tracked no usage at all, so the only grants actually usable before this fix (uncapped ones) enforced zero live quota — the audit's own "gate fix and live usage enforcement together" requirement. Fixed by adding `CapabilitySet::find_fs_read()` (the read-side twin of the already-Judged `find_fs_write()`) and a non-pure `FileSystemAdapter::usage()` seam, then applying both at every real call site. Self-red-team before implementation changed the design three ways: `usage()` unavailable fails closed rather than skipping the check; `rm` is never quota-gated (deletion only frees space); `mv` and `cp` share a call site but only `cp` is quota-checked (rename adds no usage) — proven directly by a regression pair, not merely asserted. New test block (E3-Q0-Q5, `test_mediated_shell_runner_smoke.cpp`) against a byte-exact-known mount; full suite 175/175 green. Named, not solved: `find_fs_write`/`find_fs_read`'s first-matching-grant-wins semantics (inherited from the already-Judged precedent, not new); `mount_root_usage()`'s full-rescan cost per quota-checked write (same precedent); the `mediated_python_runner.cpp` half is a mechanical pattern-match fix not locally build/test-verified this session (`AGENTENGINE_BUILD_PYTHON_RUNNER=OFF` in this environment). |
| [055](ADR-055-conflict-evidence-materialization.md) | When `merge_branch_into_parent()` reports a genuine conflict, does this codebase durably retain "both versions" the way 025 §4 requires, or does it vanish the moment the caller's local result goes out of scope? | **Judged.** It vanished (2026-08-10 gap audit #14) — `MergeConflict` was real and correctly computed, but nothing ever wrote it anywhere durable; `merge_branch_into_parent()` itself already, correctly, never touches the parent on failure (re-verified, a real invariant, not the audit's own hypothesized risk). Re-grounding found this bigger than "pick a storage location": `merge_branch_into_parent()` has zero production call sites anywhere (confirmed by grep) — `worktree_scoping.hpp`'s own top comment already says merge-on-join isn't wired for real yet — so this ADR designs what happens to conflict evidence ONCE a real call site exists, not the call site itself. Fixed: `conflicts_ref_name(parent_ref_name)` (pure, deterministic, modeled on `memory_ref_name`'s own pattern — two parents can never collide) and `materialize_merge_conflicts(...)`, a NEW function called by the merge's CALLER (never by `merge_branch_into_parent()` itself, preserving its existing contract unchanged) that writes `ours`'s and `theirs`'s ALREADY-STORED digests (no fresh blob copy needed — same content-addressed store) at `<path>.<ours_agent_id>`/`<path>.<branch.name>` into a genuinely separate `:conflicts` Ref, proven to leave the parent untouched. `ours_agent_id` is a REQUIRED explicit caller-supplied parameter (not inferable from the merge inputs alone — the audit's own "misattributes ours/theirs" finding, closed structurally). Self-red-team (inherited from the draft): writing evidence from inside `merge_branch_into_parent()` itself was considered and rejected (couples two separate concerns, breaks an already-relied-upon contract); a single shared `:conflicts` Ref per parent accumulates across every failed attempt (proven directly — an earlier attempt's evidence survives a later one), a deliberate choice with a named, unsolved retention/pruning residual. One correction to the draft found while implementing: the draft's own "SubWorktree::child_name" doesn't exist as a literal field — the real one is `SubWorktree::name`. New test file (`test_worktree_conflict_evidence.cpp`, M1-M6), full suite green. Does not build merge-on-join wiring, does not design the `/conflicts` mount's host-policy wiring, does not solve retention/pruning, does not change `merge_branch_into_parent()`'s own signature or behavior at all. **Amendment (2026-08-14):** real merge-on-join wiring -- `rt::WorkflowSupervisor` gains a `MergeOnJoinHook` (same no-ambient-authority callback shape as `checkpoint_hook_`), fired from the existing per-executor fold loop for every branch-mode executor whose reply this round was ok (025 §4: "merges back when its agent completes", NOT tied to `edge_kind::fan_in`'s own, different routing-join concept); `worktree_scoping.hpp` gains `make_merge_on_join_hook()` supplying the actual mechanics via `retry_merge_branch_into_parent()`/`materialize_merge_conflicts()`, returned as a plain `std::function` so `workflow/` still never depends on `rt/`. A new `workflow_status::merge_conflict` outcome reuses `executor_failed`/`routing_failed`'s own shape. New test file (J1-J3) is the first in this codebase to drive a branch-mode executor through the supervisor to completion, including a real end-to-end conflict with durable evidence. Full suite green. **Amendment 2 (2026-08-14):** `conflicts_mount_id()`/`conflicts_mount()` (`core/worktree.hpp`) build a real, guest-visible `Mount` for a parent's conflicts ref, mirroring `memory_mount()`'s own split -- pure binding, no capability, host still decides who gets access. Writing the FIRST test that ever read materialized evidence back through the ordinary `mount_read()` path caught a real, previously undetected bug: `materialize_merge_conflicts()` built one flat `Tree` whose entry names embedded `/` verbatim (both nested `MergeConflict::path`s and a workflow executor's own branch name, e.g. "session:s-20/agents/writer") -- silently unreachable through `mount_read()`'s segment-walk, for exactly the realistic workflow-driven case Amendment 1 just wired up. Fixed by rebuilding it with real nested-`Tree` construction (`detail::set_entry_at_path`, the same primitive `mount_write()` already uses) plus `/`-to-`_` agent-id sanitization. Test file extended (M7-M9), full suite green. |
| [054](ADR-054-tool-registry-name-keyed-resolution.md) | Given a bare string in a declarative document's `spec.tools`, does this codebase have any way to resolve it to a real `ToolDescriptor`? | **Judged.** No (2026-08-10 gap audit #4, and gap #5's name-keyed half) — 006 has zero occurrences of "registry"; 015 §1 asserts the reference exists with no mechanics behind it. Fixed with `ToolRegistry` (`core/tool_registry.hpp`), host-curated, never auto-discovered — mirrors `ChatClientRegistry`'s own shape. Capability-ceiling trust reuses the existing `check_capability_ceiling()`/`.contains()` shape at a different layer: native stays trusted by construction (the compiler is the trust boundary); `wasm_plugin`/`mcp_server`/`a2a_agent` self-reported ceilings are validated per-entry against a caller-supplied `outer_grant` (that provenance's own already-host-decided authority), excluding ONE bad tool, never a whole batch, never silently widened. `ToolTable::from_names(names, registry)` (gap #5's remaining half) is a thin consequence, delegating to the already-real `from_descriptors()`. `compile_agent_document()` gains an additive `ToolRegistry const* registry = nullptr` (Milestone 5 Phase B6's own `ChatClientRegistry const*` precedent) — `registry == nullptr` preserves the pre-existing "tools stays honestly empty" behavior byte-for-byte, re-proven, not just assumed; a supplied registry resolves plain-string `spec.tools` entries and correctly skips `{handoff: ...}` entries (014's vocabulary, out of scope). Self-red-team's own MUST-NAME 1 (a rejected tool must stay diagnosable, not indistinguishable from "never existed") closed via `exclusion_reason()`. Two new test files (`test_tool_registry.cpp` T1-T8, `test_agent_yaml_compiler.cpp` F-6), full suite green. Does not build any provider-specific ingestion path (MCP/WASM/A2A → `register_tool()` calls), does not resolve `spec.capabilities` YAML parsing, does not provide live-update/re-resolution for a stale registry snapshot, does not decide registry scope/lifetime. |
| [053](ADR-053-schedule-wakeup-standing-effect.md) | Does `rt::AgentSession` have a working `StandingEffect` producer for 019 §2's "Timer/schedule" wake row, the way it already does for background-task completion? | **Judged.** No (2026-08-10 gap audit #7) — bigger than the audit's own framing assumed: ADR-037 removed Quark's `ReminderService` entirely and `rt::` has never had ANY timer/delay primitive, not just an unwrapped one. The design draft's own §3(b) proposed a NEW CRTP policy tag, `Schedule<max_horizon_ms>` — found wrong once implementation touched real code: 007 §3 already normatively names `Schedule<max_horizon, max_active>` as a CAPABILITY, and `cap::Schedule`/`cap::decl::Schedule<...>`/`to_capability()`/`subsumes_payload()` already fully existed, simply never wired to a producer — only `CapabilitySet::find_schedule()` (mirroring `find_background()`) needed adding. Fixed: `AgentSession::schedule_wakeup(delay, label, now)` (PLAIN, UNLOCKED, same asymmetry as `start_background_task()`; `now` is REQUIRED caller-supplied, never ambient, I5) fails closed three ways (not granted / horizon exceeded / `max_active` at capacity, the `Background<max_concurrent>`/G9 precedent applied to `Schedule`) and emits `state_changed` on success (006 §6b's own normative event — NOT `tool_call_started`, a second correction to the draft's own hedged wording); `due_standing_effects(now)` is the host-polled introspection seam the draft named as "missing," read-only, mirrors `open_interactions()`. Self-red-team (inherited from the draft, unchanged): a self-firing `std::jthread` timer was considered and rejected — reintroduces ADR-037's own removed ambient-background-activity shape and a use-after-free class ADR-035 already caught once. New test file (`test_rt_agent_session_schedule_wakeup.cpp`, S1-S6), full suite green. Does not resurrect a self-firing timer, design the host poller or the resumption call's shape, solve `StandingEffect` durability generally, close `watch_resource`, or expose this as a model-callable declared tool — all named, not attempted. **Amendment (2026-08-14):** `schedule_wakeup` is now ALSO a real, model-callable tool (006 §6b/019 §2's own spec'd intent, previously unmet) -- injected into `contribution->tools` inside `run_rounds()` itself, capturing `this` directly (no existing mechanism was reusable: no reserved-tool-name interception anywhere, and ADR-028's own session-scoped-tool pattern only ever captures a `ContextProvider`, never `AgentSession`), offered only when a `cap::Schedule` grant is held. New test file (G1-G6), full suite green. |
| [057](ADR-057-agent-ask-suspend-without-deadlock.md) | Can `agent.ask()` — called from Python code running *inside* an already-dispatched `execute_code` — genuinely pause for a human answer and resume the SAME running script, without the naive port of ADR-029's suspend mechanism deadlocking `session_mutex_` against itself? | **Proposed** (design → red-team → prove complete for Design B; awaiting explicit user "Judged"). Traced the real call path (`invoke_tool()` runs at `rt/agent_session.hpp:999` while the coroutine still holds `session_mutex_`) and showed the literal ADR-029 port deadlocks by construction. Independent red-team (real source, no probe needed) **defeated two of three sketched designs on evidence already sitting in the tree**: Design A (drop the lock, block, re-acquire to signal) deadlocks anyway — `cli_chat.cpp`'s own production driver runs everything on a ONE-worker `ThreadPool` that its own header says cannot park and resume a coroutine cross-thread, so there is no second thread left to deliver the answer, on the very first outstanding ask. Design C (background thread, GIL released) requires cross-thread CPython access this embedding has never been hardened for and says so in its own comments (a real, previously-reproduced crash class); it also turned out NOT to reuse ADR-028's `Backgroundable` machinery as claimed — `ExecuteCodeTool` sets `captures_session_state = true`, which that machinery's own guard refuses to background by design. **Design B (abort-and-replay) survived red-team and was then implemented and proven for real**: a new `interaction_reason::codeact_ask`, `ExecOutcome::ask_pending`/`ask_prompt`, a `PendingCodeActAsk` suspend/resolve/replay path in `AgentSession`, and `agent.ask()` codegen wired through `_ae_internal.ask_or_raise` — the answer threaded host-side through `EffectContext`, deliberately never through the model-facing `ExecuteCodeArgs` schema (I3). Proven in `tests/test_agent_session_suspend_codeact_ask.cpp` (B1-B7, real embedded CPython): suspend-on-ask, a widened `start_run()` admission check rejecting a second `StartRun` while an ask is open (the gap Design A's own red-team finding named), correct replay of a deterministic prefix, chained multi-ask suspension under the same `interaction_id`, no regression to the no-ask path, a named fail-closed boundary for multi-call rounds, and a demonstrated (not just asserted) residual — replay repeats a script's own prior side effects, e.g. a file write before the ask, since none of that is idempotent by construction. Full regression: 964/964 build targets, 196/196 `ctest`, including ADR-029's own suite unaffected. Residuals named, not solved: side-effect repetition on replay (above); typed/schema'd answers deferred (plain string only); `Interaction::expires_at_ns` stays unwired; multi-call-round asks fail closed rather than being handled; genuine mid-script pause (Design C's actual goal) stays blocked on two separate, unbuilt runtime-substrate prerequisites (cross-thread coroutine resumption, GIL-safe multi-thread CPython embedding) — a Windows-fibers direction was named as a possible way to sidestep the GIL problem specifically, unexplored. |
| [058](ADR-058-agent-output-live-wiring.md) | Can `OutputSchema<T>`'s already-proven compile-time/request-assembly machinery (Milestone 5 Phase B5/B6) be connected to a live `AgentSession` turn, using this project's own existing type-driven-parse idiom as the validator rather than inventing a generic JSON-Schema 2020-12 engine, without silently misrepresenting 003 §4's own "one validator serves both" claim? | **Proposed** (design → red-team → prove complete for Design B; awaiting explicit user "Judged"). Corrected an earlier overstatement (this project's own Milestone 3 doc, and an earlier pass of mine, called `agent.output` a blank slate): `OutputSchema<T>` compiles to real JSON Schema text and `ChatRequest::output_schema_json` reaches real OpenAI/Anthropic request bodies — proven, not scaffolding — but none of it reaches a live `AgentSession` turn, `AgentResponse` has no field for a structured result, and no JSON Schema validator exists anywhere despite 003 §4 claiming one. Independent red-team (real source, file:line cited throughout) **defeated two of three sketched designs**: Design A (thread the whole `AgentMetadata` into `AgentSession`) is worse than drafted — the duplication hazard isn't just `tools`, it's also `max_turns` (`AgentMetadata` defaults 16, the actually-enforced `AgentSession::max_turns_` defaults unbounded) and `capability_ceiling` vs. `capabilities_`, three independently-diverged fields one drive-by edit from silently overriding the session's real, enforced state — and it doesn't even solve the type-erased-validator problem, since `output_schema_json` is already fully type-erased inside `AgentMetadata` regardless. Design C (a real, generic JSON Schema 2020-12 validator) is defeated for this pass on a definitive reading-derived finding: `json_schema_of<T>()`'s complete constraint vocabulary is `{"type": ...}` plus an optional description — zero `pattern`/length/bound/`enum` keywords ever emitted (grepped, zero matches) — so a real validator run against this codebase's own schemas would reject nothing `schema::from_json_value<T>`'s type-driven parse doesn't already reject; even the one theoretical gap (enum range) turns out doubly unenforced, since the parse side does an unchecked cast accepting any in-range integer regardless of the enum's real members. Per CLAUDE.md's "fix the spec first" rule, `003-Message-and-Content-Model.md` §4's "one validator serves both" sentence was corrected in this same pass to describe what's actually enforced. **Design B (narrow opt-in setter, matching `suspend_for_approval_`/`stream_model_calls_`'s established shape) survives**, with two named fixes: the draft's claimed precedent call site doesn't exist (only one production `register_agent<A>()` call anywhere, an offline reachability fixture that never touches a live session — this needs new wiring, not a migration), and the validator should return `result<void>` over bare `bool` for idiom-consistency. Concrete prove-phase plan (§8): request-side population scoped to `native` strategy only (the one with a real translation path); validation runs at the existing `calls.empty()` convergence branch, regardless of strategy, failing the run closed on mismatch rather than silently passing through unvalidated text; `AgentResponse` gains an additive `structured_output_json` field. Explicitly out of scope: real `tool_shaped`/`parse_and_repair` behavior; `agent.output` itself (026 §5's CodeAct module) — has nowhere to write to until this pass lands, a follow-on not part of it; streaming validation. **Implemented and proven**: real code lands exactly per the plan (one correction found while building — only ONE `ChatRequest{...}` construction site exists in the codebase, not three); full rebuild clean, `ctest` 154/154, new `test_agent_session_output_schema` 17/17 (O1-O5) — against the fully-integrated tree including ADR-057's `codeact_ask` machinery. Resolved the request-side scoping question directly rather than assuming: neither OpenAI's nor Anthropic's translation code gates `output_schema_json` on `ChatClientCapabilities.structured_output_native` (unlike `reasoning_effort` in the same function), confirming the native-only scoping is load-bearing, not redundant. One named, unconfirmed residual: the `AGENTENGINE_BUILD_PYTHON_RUNNER`-gated tree (ADR-057's own codeact_ask test) wasn't rebuilt against this change — reasoned low-risk (additive-only, gated behind an unused setter) but not directly verified. |
| [059](ADR-059-invoke-agent-tool-capability-attenuation.md) | Can `invoke_agent_tool()` — which minted a target agent's own full, unattenuated `capability_ceiling` via `grant_root()` unconditionally, ignoring what the calling `EffectContext` actually holds, a textbook I2 ambient-authority hole — be fixed to grant only the intersection of what the caller holds and what the target declares, by reusing `CapabilitySet::attenuate()` (ADR-009, already proven), without breaking the one existing test or being defeated by `attenuate()`'s own capped-vs-uncapped WIDENING rule? | **Proposed** (design → red-team → prove complete; awaiting explicit user "Judged"). Confirmed the bug matches its own draft exactly (`ctx.capabilities` was read nowhere in the old body) and re-confirmed directly that `invoke_agent_tool()` has zero production callers today (`native_jail/tool_bridge.hpp` and `core/run_event.hpp` both say so in their own comments, written before this fix) — so this is real, purely forward-looking hardening for the future `agent.spawn`/MCP-tool-exposure path this glue would back, not a live vulnerability closed today. The main attack — does the capped-vs-uncapped WIDENING rule reject the fix's own realistic case — was run to ground: every `cap::decl::*` declaration tag (`FsRead<Mount>`, `FsWrite<Mount>`, `NetOut<Host>`, `Exec<Profile>`) has no cap/quota NTTP parameter at all, and every `to_capability()` overload compiles a nullopt/empty cap on every scalar field, so `meta.capability_ceiling` is structurally uncapped for every agent declarable with today's DSL — meaning the WIDENING rule only rejects when a caller's own held capability is itself capped, exactly the already-judged, already-proven ADR-009 behavior (R-C3), not a new defect. **The fix survived red-team as originally proposed, unmodified** — implemented as `CapabilitySet::attenuate()` against `ctx.capabilities`, failing closed with a dedicated `agent_call.no_caller_capabilities` code when `ctx.capabilities == nullptr`, reusing `tool_pipeline_detail::make_error_result()` (the same helper ADR-029's `make_denial_result()` already uses). `tests/test_agent_tool_invocation.cpp`'s original two cases were fixed to attach a real, covering `CapabilitySet` (their default-nullptr `ctx.capabilities` would otherwise now be rejected before ever reaching what they were written to prove), and three new cases were added: a caller holding less than the ceiling rejected (`capability.attenuation_not_subsumed`), a caller holding more than needed still succeeds with the derived grant proven bounded to the target's own ceiling size (no leak-through), and the null-capabilities fail-closed path. Full evidence: clean Windows/MSVC build, targeted test `ALL PASS`, full `ctest` sweep **154/154 passed, 0 failed**. Named residual, found during red-team, not anticipated in the design: the declarative `cap::decl::*` surface has no syntax for a capped ceiling entry at all, so in practice a caller today can only satisfy the new gate on scalar axes by holding an equally-uncapped grant — correct per ADR-009's own rule, but a real limitation of the surrounding capability-declaration system worth its own future ADR if capped declarative ceilings are ever added. |
| [060](ADR-060-tool-call-progress-reporting.md) | Can `EffectContext` carry a real, call-scoped `report_progress()` channel that a `Tool::invoke()` calls synchronously, routing through the session's own `emit_run_event()` with the correct `call_id`, without leaking a stale binding between calls and without reopening the ADR-028 dangling-reference/data-race hazard class through a different door? | **Proposed** (design → red-team → prove complete; awaiting explicit user "Judged"). The design as originally drafted did NOT survive red-team as-is — a real, must-fix hazard was found and corrected before implementation. Confirmed clean: the core mechanism (bind `effect_context_.report_progress` immediately before, reset immediately after, at all three real `invoke_tool()` call sites in `rt/agent_session.hpp`) is sound on the synchronous, foreground path — `invoke_tool()` passes `EffectContext` by reference, never a copy, and no `co_await` sits inside any bracket, so two sequential calls in one round never leak a binding. The sharpest question (a tool stashing `ctx.report_progress` into a member and calling it later) is confirmed-clean: only a session-scoped stateful tool (ADR-028's `make_tool_descriptor_with_invoke`) could ever capture something reaching the session, and that construction is already structurally forbidden from ever being `Backgroundable` — so a stashed closure can only replay synchronously, on the session's own `session_mutex_`-serialized thread, producing at worst a stale-but-real `call_id`, never a memory-safety hazard. **The real finding**: `tool_pipeline.hpp::background_task()` — the one function that ever detaches a `std::thread` to run a `Backgroundable` tool's `invoke()` — takes `EffectContext` BY VALUE, and `AgentSession::start_background_task()` is documented "PLAIN, UNLOCKED," explicitly able to race a live `report_progress` bracket on a different thread of control. An ordinary, non-malicious call to `ctx.report_progress()` from a backgrounded tool could therefore reach back into the originating session's `emit_run_event()` from off-thread, racing `run_event_seq_by_run_` (an unlocked `std::unordered_map`) — a confirmed, concrete hazard, not the same class as the OTHER, already-accepted `EffectContext` races on passive data. **Fixed structurally**: `background_task()` resets its own local copy's `report_progress` to the no-op unconditionally before step 8 ever runs, matching ADR-028's own "closed here structurally, not by convention" precedent. Proven, not just argued: a new `tests/test_agent_session_tool_call_progress.cpp` (P1-P3, D) shows a real `tool_call_delta` event with the correct `call_id`/text (P1), a silent tool producing no event (P2, positive control), two sequential calls in one round each getting their own correctly-tagged `call_id` (P3), and — the regression check for the must-fix finding — a `Backgroundable` tool's `report_progress()` call from its own detached thread never reaching the caller's closure (D), verified non-vacuous by temporarily reverting the fix and confirming check D fails exactly as predicted, then restoring it and confirming all 13 checks pass. Full evidence: clean Windows/MSVC build (zero errors, no new warning class), full `ctest` sweep **155/155 passed, 0 failed**, run three times across two rebases (`main` advanced mid-pass; re-verified against the fully-integrated tree both times). Residuals named, not solved: `agent.progress`'s own CodeAct/`_ae_internal` wiring; structured/typed progress payloads; real `StandingEffect`/background-task progress delivery (now backed by a structural, not merely documented, absorption guarantee); end-to-end MCP-progress/AG-UI-SSE projection of a real (not hand-constructed) `tool_call_delta` event. |
| [040](ADR-040-wasm-tool-pipeline-bridge.md) | How does a loaded `WasmBackend` component's tools become reachable through the real 006 §3 tool pipeline, without opening a new authority-crossing hole between an agent's capability ceiling and a plugin's own capability grant? | **Proposed**, awaiting explicit user "Judged." `backends/wasm/wasm_tool_bridge.hpp`'s `wasm_tools_as_descriptors_from()` mirrors `mcp_tool_bridge.hpp`'s already-proven shape: each adapted `ToolDescriptor` is gated behind exactly one `cap::ToolCall{plugin_id + "::" + tool_name}` — an agent-level gate deliberately independent of `WasmBackend::invoke_tool()`'s own, already-proven `operator_grant`/`manifest.requested_capabilities` check (ADR-010 §3.2/§3.3); two authority layers at two different points, not a double-binding conflict. A pre-implementation design-review pass caught two real gaps before any code was written: a delimiter-collision spoofing path (`PluginManifest.id` is plugin-author-supplied, not host-assigned, so naive concatenation could let two different `(plugin_id, tool_name)` pairs collide onto the same capability name — closed by rejecting `"::"` in either input, fail closed) and a missing `captures_session_state = true` (the adapted closure holds a `shared_ptr<WasmBackend>` into live, mutable instance state — exactly ADR-028's hazard). A `Media`/blob wasm tool result fails the call closed (`wasm.tool_result_unsupported_content`) rather than silently dropping it — `tool_pipeline.hpp`'s own step 9 has no path today for a success reply to become anything but one `Data` item, so this is a fresh output-direction scope cut, not a reuse of ADR-010 §3.1's input-direction blob residual. `union_codeact_tools()` (`core/codeact_tool_union.hpp`) extended with wasm as a fourth source, same generic collision-detection logic. Proven end-to-end in `tests/test_wasm_tool_bridge.cpp` against the real compiled `ae:tool` fixture already used by `test_wasm_backend.cpp`: a wasm-hosted tool reachable through the real `agentengine::invoke_tool()`, the I2 gate denying independent of the wasm instance's own fully-granted `operator_grant`, and cross-source collision detection firing against a wasm-sourced name clash — plus pure-logic unit tests for the `is_error`/`Media`-content mapping paths the real fixture has no tool to exercise end-to-end. Residuals named, not closed: no production loader exists anywhere in this repo yet (this bridge takes an already-loaded handle, same assumption `mcp_tools_as_descriptors()` makes toward an already-connected `McpClient`); `PluginManifest.id` uniqueness is unenforced; `WasmBackend::destroy()` racing a live `ToolTable` built from this bridge is a named, not solved, caller-responsibility race; manifest approval/signature verification remains ADR-010 §5 F6's unresolved residual. |
| [061](ADR-061-host-provided-inbound-transport.md) | When a consumer-supplied host owns the HTTP transport and hands the engine an already-parsed request, does the engine derive the request's `Principal` by validating credentials itself, or accept one the host asserts? | **Split by tier (2026-08-20). Tier 1 — Judged** (§11, project owner sign-off): the client role needs no host at all, because `conformance client --command` spawns our binary (`tools/mcp_conformance_client.cpp`). §10b's prove phase ran for real against the official `@modelcontextprotocol/conformance@0.2.0-alpha.11` CLI: all 6 falsifiable claims CORRECT, two with real executed teeth (breaking `derive_param_headers` drops the passing count by 30, reverted; a planted canary proves the stdout/stderr leak-scan is non-vacuous). **Honest gate result, not smoothed over: non-auth conformance is 75/75 (100%), but 011 §10 G2 also names the `auth` suite, and this driver has zero OAuth machinery — auth is 3/49 (~6%), so G2 is NOT met.** A real defect was found and fixed as a byproduct: `perform_http_exchange`'s chunked-decode gap (ADR-011's own 2026-08-19 addendum). **Tier 3 (host-fronted server role) — Judged** (§45, project owner sign-off): after two red-team rounds returned 33 findings against the original identity-exchange design (it laundered a host-chosen identity into `verified_by_engine`) and iteration 3 re-scoped the work, the session-side admission mechanism and both bearer-credential-to-`RequestAuthority` bridges (`rt::`/`mcp::`) are implemented and proven for real (§20-§44, 207/208 tests green) — including `A2aServer::send_message()`'s and `McpServer::dispatch()`'s per-request `CapabilityGrant` wiring. Tier 3's first red-team round also found **four live security defects in shipped M7 code**, all fixed and proven (`tests/test_task_principal_binding.cpp`, 19 checks): unauthenticated cross-principal `GetTask`, an admission fail-open on every inbound A2A message, `mt19937_64` task ids, and audit records with no identity. R27's `approve_` finding (a principal-aware `ApprovalDecider`) was separately designed and proven (§46-47, 208/208 green) after the Tier 3 sign-off and awaits its own judge pass. **What Tier 3 does NOT close**: the actual test-fixture listener (§6, over `quark::pal::tcp_listen`) needed to run `conformance server`/`a2a-tck` against a real URL was never built in this ADR, so 011 §10 G1 and 012 §8 G1 remain unmet; other named residuals (007 §5's policy engine, `A2aServer::context_id_`, `RunEventProjector::thread_id_`, ADR-039 §3e's reference host-side example) are untouched. One measured result stands: a 16-byte authority handle puts `Ask<StartRun, AgentResponse>` at 104 bytes against `kMaxPayload`'s 192. |
| [063](ADR-063-retrieval-augmented-context-provider-shape.md) | What should a retrieval-augmented-generation `ContextProvider` and its storage seam look like — `Embedder`/`VectorIndex`/corpus mounting/citation rendering — without repeating Memory's own historical cross-tenant and citation-forgery bugs? | **Judged** (2026-08-19, project owner sign-off; design → red-team → two implementation passes → second red-team pass → fixes, all same day). Real, compiled, tested code for every piece §2 designs: `Embedder`/`BruteForceCosineIndex`/`corpus_scope`/`CorpusChunkRecord` foundation, `DiskCorpusSource` folder-mount ingestion with re-mount dedup, `OpenAIEmbedder` (OpenAI/OpenRouter, live-tested against real `openrouter.ai`), `VectorRagContextProvider` tying it together with multi-tenant isolation and citation-forgery-resistant rendering. §3 claims 2, 3, 4, 5, 6 and finding 7 all CORRECT with real executed evidence; claim 1 (brute-force latency) measured for real (Release/clang: ~248µs/~1.45ms/~7.6ms at n=200/1000/5000, dim 1536) now has a real RFC 023 §3 budget row too (added 2026-08-19, project owner's own choice: ≤500µs, Goal, matching "Context assembly"'s existing row) — n=200 PASSES, n=1000/5000 are genuine, accepted Goal-tier MISSES (023 §6: warns, doesn't hard-fail); claim 7 (Vulkan) deliberately deferred. A second, independent red-team pass against the real implementation found and fixed 5 real issues: a `rag_corpus_key()` collision (two differently-scoped mounts could alias under an unescaped `:`-joined key — fixed via length-prefixed encoding), a `last_user_text()` path that could let model output reach the embedder as a live network payload (I3 — fixed by walking to the last real `role::user` message, also fixed in the inherited `memory_provider.hpp` copy), an unbounded chunk size for no-whitespace content, missing `VectorIndex` dimensionality validation, and a `CorpusChunkRecord` JSON field that silently defaulted instead of rejecting. Full suite 194/194 (`ctest -LE live-network`) throughout. `recall(query)`'s `invoke` gap (synchronous-tool-vs-async-embedder) is now CLOSED — see ADR-064's own row: `rt::drive_leaf_task()` + `Embedder::synchronous_leaf` implemented and tested, `recall` now genuinely invocable end-to-end for any `synchronous_leaf = true` conformer. |
| [064](ADR-064-recall-tool-sync-invoke-vs-async-embedder.md) | Can a synchronous `ToolDescriptor::invoke` safely drive `Embedder::embed_batch()`'s `task<T>` to completion, closing ADR-063's `recall(query)` gap, without changing `ToolDescriptor::invoke`'s signature (and therefore 25+ call sites across the tool pipeline, MCP/A2A servers, and the native-jail CodeAct bridge)? | **Judged (2026-08-19, project owner sign-off) — Design B implemented and tested.** `rt::drive_leaf_task<T>()` (`rt/drive_leaf_task.hpp`) drives a leaf `task<T>` in exactly one `resume()`, per the design's own corrected (post-red-team) contract; `Embedder`'s new REQUIRED `synchronous_leaf` trait (`core/embedder.hpp`) gates its use — all 4 real conformers migrated (`OpenAIEmbedder` declares `true`, justified by its body never awaiting anything; the 3 test mocks declare `true`, each genuinely leaf by inspection). `VectorRagContextProvider::recall`'s `invoke` (`vector_rag_context_provider.hpp`) now genuinely drives `embedder_.embed_batch()` + `index_->search()` + the SAME `render_scored_chunk()` helper `on_context()` already used, end-to-end, for any `synchronous_leaf = true` conformer; a conformer declaring `false` still gets the original fail-closed `failure_class::contract` error, unchanged (no regression). Proven by a new, dedicated `tests/test_rt_drive_leaf_task.cpp` (D1-D5: a conforming leaf task drives in one resume(); the double-wrapped `result<T>` is preserved, not flattened, both for the ordinary inner error channel and a genuine outer task-level fault; nested `task<T>` composition still resolves in one resume(); a task genuinely parked on a contended `AsyncMutex::lock()` is reported as a violated `synchronous_leaf` contract — reproducing ADR-037's own addendum finding directly against `drive_leaf_task()` itself, not only through `ThreadPool` — and the abandoned contender does not corrupt the mutex for later legitimate use) plus real end-to-end coverage added to `tests/test_vector_rag_context_provider.cpp` (R8/R8b/R8c: `recall()` invoked with well-formed args against a `synchronous_leaf` `MockEmbedder` returns the real ranked, cited chunk text, not the old stub error; R9: malformed args still rejected before any embedder/index work). Full suite 195/195 (`ctest -LE live-network`), zero regressions. Rejects Design A (async the whole tool pipeline, too large a blast radius, 25+ call sites) and defers Design D (a real async executor) as future work; Design C (`Backgroundable`) remains a documented, not-yet-implemented complementary follow-on for slow-backend deployments. Separately, the `rt::ThreadPool`+`rt::AsyncMutex`+`rt::AgentSession` reentrancy/UB hazard this ADR's own red-team found in already-shipped code is FIXED (`rt/thread_pool.hpp`, `tests/test_rt_thread_pool.cpp` T6, documented as an addendum to ADR-037's own index row above), independent of Design B. **A second red-team pass against this real implementation (2026-08-19, no blocking defect found)** fixed one real gap (`drive_leaf_task()` now wraps `resume()` in the same try/catch as `take_value()`, matching `ThreadPool::run_job()`'s own defense-in-depth exactly), corrected an incomplete `this`-capture-safety comment (now backed by a new test, R12, driving `recall` through the real `make_shared<ProviderT>`-backed wiring), narrowed an overclaimed thread-safety line (`IndexT` sharing across a future concurrent-writer ingestion path named as a real-but-currently-unreachable residual), and added R13 (`recall` against a genuinely empty index). Both of those test gaps were closed the same day: R14 proves a stale, unbacked index entry reached through `recall` is silently skipped, not a whole-call failure; R15 drives 8 real threads calling `recall`'s invoke concurrently against the SAME provider instance, all correct, stable across 5 repeated runs (scope-limited to no concurrent writer, since no writer/ingestion path exists in the tree yet). **The two remaining named residuals were then closed for real (2026-08-19)**: `BruteForceCosineIndex` (`core/vector_index.hpp`) gained a real `std::shared_mutex` (unique lock on `add_batch()`, shared lock on `search()`/`contains()`/`size()`) — no longer "currently unreachable," genuinely thread-safe under concurrent read+write, proven by a new `test_vector_index.cpp` block with a real writer thread interleaved with 4 reader threads (200 writes, zero corrupted reads, stable across 5 runs); and a new `NonLeafEmbedder` test conformer (`synchronous_leaf = false`) drives `recall`'s invoke through the `if constexpr` FALSE branch for the first time via a real instantiation (R16), confirming the fail-closed fallback works exactly as designed, not merely compiles. |
| [065](ADR-065-tool-optimizer-provider.md) | Can a large MCP/plugin/agent tool surface stay under the ~200k-token schema budget that degrades tool-selection accuracy, via an on-demand `ToolOptimizerProvider` gate rather than hand-pruning? | **Judged** (2026-08-20). As-built: decision, real implementation, and tests landed together (matching ADR-024's role for `mount_skill`) — not a pre-implementation contested-design cycle, since issue #15's own design draft (§6) already established this changes nothing about `assemble_context`/`ContextProviderDescriptor`/`ContextProvider` themselves. Reuses `core/codeact_tool_union.hpp`'s cross-source collision machinery rather than reimplementing it; inherits ADR-024 §8's mount-visibility cadence invariant and its unmount gap. Not a reopening of OQ-18 (already resolved) — see the ADR's own §1. |
| [066](ADR-066-context-provider-attribution-provenance.md) | Should `ContextContribution` provenance be stamped by each contributor's own discipline (MAF's shape), or enforced structurally at `assemble_context()`'s seam? | **Judged** (2026-08-21, project owner sign-off). Implemented (`ContributorProvenance` in `content.hpp`, `Message`/`ToolDescriptor::attribution`, `HasContextProviderName`/`ContextProviderDescriptor::name` in `context_assembly.hpp`, stamping logic in `assemble_context()`), proven (`tests/test_context_provenance.cpp`, 16/16, Windows/MSVC; full-tree rebuild 0 compile errors; `ctest` 181/191, the 10 not-run are pre-existing/unrelated CPython-embedding targets). Decision: stamp centrally at `assemble_context()`, requiring a declared `name` on every `ContextProviderDescriptor` (mirrors ADR-033's `HasMiddlewareName`) — cannot be skipped by a non-cooperating contributor the way MAF's own per-provider self-stamping can. **A real finding surfaced only DURING implementation**: the draft's own `content_origin` fix ("any contributor-sourced message maps to a provider-derived origin, regardless of what the contributor set") is WRONG as literally stated — it would have regressed `SkillsProvider`'s already-shipped `content_origin::system` advertisement. Fixed by narrowing the check to `content_origin::user` specifically (the one origin only genuine, verbatim-replayed history may claim), leaving every other origin exactly as the contributor set it. Also surfaced, but NOT closed by this ADR (named residual): `HistoryProvider<Summarize<...>>`'s synthesized summary message inherits an unmarked `content_origin::assistant` with nothing distinguishing it from a real assistant turn. Creates and closes `OpenQuestions.md` OQ-22; does not reopen OQ-18. Foundational to ADR-067. **Wired into `rt::AgentSession` (2026-08-20)**: `tests/test_rt_agent_session_context_provenance.cpp` (13/13) proves a real, composed multi-provider `HistoryProviderT` slot carries attribution all the way to the real outbound `ChatRequest`, zero `agent_session.hpp` changes needed. |
| [067](ADR-067-middleware-turn-point-pre-model-enforcement.md) | Does closing 017 §4's `pre_model` filter gap need a new "terminal `ContextProvider`" concept, or does it reuse 002 §5's already-declared, unwired `Middleware` `turn` point? | **Proposed** (design → red-team → prove complete; awaiting explicit user "Judged"). Implemented (`core/turn_middleware.hpp`: `TurnContext`/`ToolSurfaceView`/`redact_subspan()`/`Compactor<N>`/`run_turn_middleware_chain()`), proven (`tests/test_turn_middleware.cpp`, 22/22, Windows/MSVC; full-tree rebuild 0 errors; `ctest` 182/192, same 10 pre-existing not-run targets as ADR-066). Decision: wire the existing `turn` point (002 §5), reusing 017 §4's verdict vocabulary — closes `pre_model` only, explicitly NOT `post_model`. FATAL finding fixed structurally: `ToolSurfaceView` never exposes a mutable `ToolDescriptor`, only narrow non-`invoke`-touching mutations on stable handles. **Two real findings surfaced only DURING implementation**: (1) the draft's claimed ADR-033 before/after onion reuse doesn't fit — the turn point has no real inner action to sandwich (unlike the model call), so what's actually built is a single forward pass, first-deny short-circuits; (2) `ToolSurfaceView` as drafted (each middleware constructs its own) silently disconnects `redact()`/`reorder()` from `finalize()` across separate instances — found by the test file's own first, failing version — fixed by giving `TurnContext` one shared `tool_surface` instance. Taint boundary enforced via subtractive-only `redact_subspan()`, property-tested against 8 offset/length cases including adversarial ones. `Compactor<N>`'s "never touches `history[]`" claim proven both structurally (the type carries no reference to it) and behaviorally. `ToolSurfaceView`'s guarantee is scoped to middleware using ONLY its sanctioned API, not one bypassing it into the raw `TurnContext::assembled` reference (named residual, not closed). Depends on ADR-066 (Judged); explicitly does not reopen OQ-18. **Wired into `rt::AgentSession` (2026-08-20)**: `AgentSession::set_turn_middleware_hook()` runs the chain once per round, confirmed as the ONE genuine `pre_model` seam in the file (the other two `on_context()` call sites never reach the model). `tests/test_rt_agent_session_turn_middleware.cpp` (8/8): a redacting middleware's decision reaches the real outbound request; a denying middleware fails the round with the model NEVER called. |
| [068](ADR-068-runtime-secret-quarantine-host-delegated-detection.md) | When a secret leaks into ordinary content at runtime, does AgentEngine own detection and audit durability itself, or delegate both to the host through a declared seam? | **Proposed** (design → red-team → prove complete; awaiting explicit user "Judged"). Implemented (`trust/secret_quarantine.hpp`), proven (`tests/test_secret_quarantine.cpp`, 24/24, Windows/MSVC, `ctest` 2/2 clean). Decision: neither detection (`SecretDetector`) nor audit durability (`QuarantineAuditHook`) is engine-owned — both are host-injected callbacks, `nullptr`-by-default, same idiom as `ApprovalDecider`/`MiddlewareTraceHook`. One FATAL finding fixed pre-implementation: the original design leaned on `invoke_tool()`'s in-memory-only audit record for accountability — fixed via host-delegated durability. **A second, real finding surfaced only DURING implementation, not anticipated by the ADR text**: the design's own "quarantine() grants a capability in the same step" mechanism is not implementable against the real `CapabilitySet` (empty by construction, no incremental-grant method, `grant_root()` explicitly "never reachable from anything derived from model output") — and would have been I3-unsafe regardless, since the agent-initiated tool's argument is model-supplied. Fixed by splitting grant-eligibility bookkeeping (host-only, never touched by model-reachable code) from any actual grant (entirely the host's own `CapabilitySet::grant_root()` call, unchanged). Mint-at-runtime `SecretRef` naming is content-addressed (SHA-256 via the existing audited `trust::hmac_sha256`), giving cross-contributor dedup for free without needing ADR-067's chain mechanism. Independent of ADR-066/067. **Wired into `rt::AgentSession` (2026-08-20)**: `QuarantineToolProvider` (new, `trust/secret_quarantine.hpp`) is a real `ContextProvider` conformer contributing the `quarantine_secret` tool — occupies `AgentSession`'s `HistoryProviderT` slot directly, zero `agent_session.hpp` changes needed. `tests/test_rt_agent_session_quarantine_tool.cpp` (5/5) drives a real round where the model calls the tool through the actual `invoke_tool()` pipeline; a real bug in the test's own first version (only scanning top-level `ContentItem`s, missing the `ToolResult`'s nested content) silently found neither the leak nor the marker until fixed. |
| [069](ADR-069-content-triggered-model-response-replay.md) | When a model response has already committed content that violates policy, does AgentEngine discard-and-retry inside the existing `after_model` point, or leave remediation entirely to the host? | **Judged** (2026-08-21, project owner sign-off). Implemented (`core/content_replay_gateway.hpp`: `ContentReplayGateway<Inner>`/`ContentReplayTrigger`/`ContentReplayTraceHook`/`corrective_message()`), proven (`tests/test_content_replay_gateway.cpp`, 30/30, Windows/MSVC; full-tree rebuild 0 errors; `ctest` 183/193, same 10 pre-existing not-run targets as ADR-066/067). Decision, corrected during implementation: rather than extending `MiddlewareModelCallGateway`/`ModelCallContext` (ADR-033/036, shipped and Judged) with new fields, built a SEPARATE wrapper composing over any `ModelCallGatewayLike` — the same compositional shape `MiddlewareModelCallGateway` already uses over `ModelCallGateway` — leaving ADR-033/036's code untouched. **A real finding surfaced only DURING implementation**: the amended retry request must never re-include the discarded response's own content — doing so would re-send whatever got a response discarded (e.g. a secret) back to the vendor a second time, inside the very call meant to correct it; proven directly against the real captured request in tests, not just documented. Bounded two independent ways (per-trigger-site AND session-lifetime, the latter persisting across separate `call()` invocations on one gateway instance), both proven. Streaming excluded structurally (no `chat_stream()` method exists on the type at all, not a documented-only rule). `TokenBudget` accounting verdict narrowed to INCONCLUSIVE: every attempt's real usage is now observable via `ContentReplayTraceHook`, but `AgentSession`'s own existing budget enforcement is not wired to consume it (named residual). Independent of ADR-066/068 (confirmed: no shared includes). **Wired into `rt::AgentSession` (2026-08-20)**: `ContentReplayGateway<Inner>` plugs directly into the existing `ChatClientT` slot (`ModelCallGatewayLike`), zero core changes. `tests/test_rt_agent_session_content_replay.cpp` (8/8): the gateway is transparent to the turn loop, durable history holds only the final kept response. Relationship to ADR-067, now answered with real positive evidence: `tests/test_rt_agent_session_turn_and_replay_composition.cpp` (4/4) runs both mechanisms in ONE session and proves they compose without interference (turn-middleware shaping applies uniformly across every replay attempt) — whether they SHOULD be unified remains open. |
| [070](ADR-070-host-configurable-responsibility-boundary.md) | Should AgentEngine keep enforcing one strict safety posture everywhere, or name a formal seam for shifting part of that responsibility to the consumer dev, and where does that seam stop? | **Judged** (design → red-team → prove complete; approved by project owner, 2026-08-21). Names the "Delegated Decision Seam" pattern (explicit opt-in, fails closed/safe when unset, narrows-or-decides-among-already-possessed-authority only, host code never model output, always audited) already implicit in `ApprovalDecider`/`SandboxBackend`/ADR-068, and states what must NOT become one (the `text_derived` closed declassifier allowlist, `Tainted<T>::unsafe_view()`, ADR-033's middleware closure, `SpawnBudget`/`SpawnCostBudgetActor` attenuation-only, I2's empty-by-default guarantee, `check_workflow_executable()`'s agent/sub_workflow rejection — OQ-19 stays "document only, do not implement yet"). Implements four bounded instances closing real, previously-named gaps: (a) `PolicyDecider` — a host-injectable graduated resolution for `Agent<...>`'s `policy_driven` approval mode, proven to never bypass capability binding or the `text_derived` gate (`tests/test_tool_pipeline.cpp`, 6 new checks; `tests/test_rt_agent_session_suspend_approval.cpp` SU7/SU8); (b) `AgentSession::expired_interaction_ids()`/`set_interaction_expiry()` — closes ADR-029 §6's named "nothing checks `expires_at_ns`" gap as a host-driven query pair, not an engine-internal timer (no wall clock is wired in this project) — reuses the existing `resolve_interaction()` path, no new resolution mechanism or race (SU9); (c) `WorkflowSupervisor::token_budget_unenforced()` — an unconditional, deterministic diagnostic closing the silent gap that `TerminationBound.token_budget` is host-settable but never engine-checked (`tests/test_rt_workflow_supervisor.cpp`, 3 new checks); (d) formalizes ADR-066 §7's already-named single-provider-path attribution gap as a deliberate, documented two-tier contract (documentation-only). Full Windows/MSVC rebuild: zero compile errors; full `ctest`: see the ADR's own §5 for the exact count. |
| [071](ADR-071-native-unsandboxed-process-execution-providers.md) | Should AgentEngine ship native, unsandboxed host-process-execution providers, and how do they stay compatible with I2/I3 and worktree confinement without a sandbox jail? | **Judged** (design → red-team → prove complete; approved by project owner, 2026-08-22). Implements `NativeShellProvider`/`NativeBashProvider`/`NativePythonProvider`/`NativeNodeProvider` (`src/backends/native_process/`, a new Tier-2 backend gated behind `AGENTENGINE_WITH_NATIVE_PROCESS`, OFF by default, never linked into `agentengine_shell_runner`/`agentengine_python_runner`), the next bounded instance of ADR-070's Delegated Decision Seam. Decision: capability-first discovery (Design B) — a host-granted `cap::NativeExec` pattern is the ONLY thing that determines what `scan_path()` may return or a tool call may invoke; PATH-scanning is real but can only narrow "what's on PATH" down to "what's on PATH AND already authorized," never the reverse (proven against a real, ungranted-but-present executable, `test_native_path_scan.cpp` R-P3). Worktree confinement reuses ADR-014's handle-anchored primitive for argv pre-validation; `real_run()` re-verifies the grant fresh per invocation against the live `EffectContext`, never a cached prior-turn scan. Narrowly amends CLAUDE.md's/`008-Sandbox-and-Isolation.md`'s "embedded CPython, never a second runtime" bullet (ADR-037 precedent for editing a locked decision): scoped to the mediated code interpreter specifically, since `NativePythonProvider` is reached only through its own distinctly-named tool, never `execute_code`. Two real findings caught and fixed before evidence was finalized: an argv-shape false positive live-wiring `cmd.exe` (`"/c"` misread as a path-escape attempt) and an unclosed family-distinctness gap (`assemble_context()` has no tool-name collision check) closed via a compile-time `requires`-clause proof (`compile_fail/native_capability_announcer_rejects_duplicate_family.cpp`). 99 new checks across 6 test executables, 227/227 full suite (`ctest -j1`), zero regressions; default build confirmed zero-footprint with the flag unset. **Honestly disclosed, not-eliminated residual:** a spawned child's own subsequent raw syscall on a string-passed path argument is a genuine TOCTOU window this mechanism cannot close (unlike `open_within_mount_root`'s own callers, which use the verified HANDLE directly) — matches this project's disclosure norm for ADR-041's accepted residual rather than overclaiming a hard boundary. POSIX (`posix_spawn`) implementation is design-only, no Linux env available to verify against. |
| [072](ADR-072-external-host-discovered-skill-sources.md) | Should AgentEngine auto-scan the host machine for skills belonging to OTHER installed AI coding tools (Claude Code, GitHub Copilot, OpenAI Codex), and if so, how? | **Proposed** (design → red-team → prove phases complete; awaiting project-owner "Judged" sign-off, matching ADR-070/071's own flow). Reuses `009-Plugin-and-Extension-System.md` §8's `SkillSource`/`SkillsProvider` machinery unchanged. Checked against ADR-070's Delegated Decision Seam pattern (§4) and named against ADR-066's provenance mechanism, which this ADR's own residual falls short of (§7). `kKnownRoots` backed by dated, sourced research (`docs/research/2026-08-21-external-ai-tool-skill-conventions.md`). |
| [073](ADR-073-unified-streaming-gateway-call-stream-tool-argument-result-streaming-ask-stream.md) | Can four independent streaming gaps (gateway-typed sessions can't stream, tool-call arguments/results are invisible mid-flight, the quickstart `Bundle` facade has no streaming entry point) plus `EffectContext::report_progress`'s text-only limitation be closed as one coordinated design, without reopening I2/I3/004 §4's no-silent-substitution rule? | **Judged** (design → red-team → prove complete; approved by project owner, 2026-08-22). Five pieces: `ModelDelta` becomes a real discriminated union (tool-call-argument fragments streamed from both OpenAI and Anthropic backends); `ToolCallFinished` widened to carry the real `ToolResult`; `EffectContext::report_progress` widened from `std::string_view` to `ContentItem`, taint-forced recursively; `ModelCallGateway::call_stream()` — a new, `chat_stream()`-shaped, commit-gated streaming entry point (`ModelCallGatewayStreamLike`, a separate optional concept, not a widening of `ModelCallGatewayLike`); `Bundle::ask_stream()`, reusing `ask()`'s own proven bounded-single-`resume()` contract via a driver+relay thread pair. **Five independent adversarial red-team rounds** (three against the first three pieces, two against the gateway/Bundle pieces) found and fixed 20+ real findings before any `prove` work started, including one architecturally significant miss surviving two full rounds (Piece B silently scoped to OpenAI only) and a real deadlock risk + a reintroduced double-resume hazard in the gateway/`Bundle` pieces' first sketch. `prove` then caught 4 MORE real bugs no red-team round found: a stale lambda signature, a double-move hazard, `Bundle::ask_stream()` never enabling `stream_model_calls_` (would have silently returned an empty stream), and a stale test assertion in the Anthropic translation suite. New, non-vacuous tests added (`test_model_call_gateway.cpp` G9-G11 proves the commit gate actually gates: a post-commit failure never falls over to a configured fallback). Full rebuild zero errors (818 targets, then a second `AGENTENGINE_WITH_HTTPS` tree closing every remaining compiler-verification gap); `ctest` 196/196. **First live-network test of `Bundle::ask()`/`Bundle::ask_stream()` ever written**, run against real OpenRouter: both succeed end to end, `ask_stream()` delivers 4 real live chunks, a wrong credential is correctly rejected through the full stack. See `docs/planning/unified-streaming-design-draft.md` for the complete, uncompressed record of every finding and fix. **NOTE (merge, this session): `Bundle::ask_stream()` was re-ported onto ADR-072-era's `Bundle` redesign (variadic `.store()`, `ComposedQuickstartSessionBuilder`, `.max_turns()`/`.token_budget()`) during reconciliation of two divergent sessions — see `session_builder.hpp`'s own top comment, finding 13, for the merge account.** |
| [074](ADR-074-composed-context-provider-consolidation.md) | Should the three overlapping `ContextProvider` composites (`HistoryAndSkillsProvider`, `ComposedContextProvider`, `LazyComposedContextProvider`) be consolidated into one type? | **Judged** (2026-08-23, project-owner sign-off, same-session decision — not a full multi-agent design→red-team→prove→judge cycle; the two mechanisms merged here were already separately red-teamed and proven on `LazyComposedContextProvider`). Closes `docs/planning/2026-08-22-component-role-audit-tracker.md` Findings A (redundant `HistoryAndSkillsProvider`, deleted) and B (`ComposedContextProvider`'s fork-aliasing bug, I1/I4-adjacent — closed at the source via move-only semantics). Verified non-breaking before implementation: zero existing `.fork_from()` caller touches any of these types. **A real design conflict found DURING implementation, not anticipated in the plan**: an early attempt auto-engaged the default constructor whenever every `Ms` happened to be default-constructible (matching the old eager type's ergonomics) — this silently broke `ComposedQuickstartSessionBuilder::build()`'s own `.engage()` call for any real caller choosing all-default-constructible providers (`already_engaged` failure), caught by `tests/test_session_builder.cpp`'s own pre-existing B22 exception-safety regression test cascading into 5 failures. Fixed by making default construction ALWAYS start empty/unengaged, unconditionally — four call sites that relied on the old auto-engage behavior needed one explicit `.engage()` call added each. Proven: `tests/test_composed_context_provider.cpp` (new Part 3: `engage()`, move-only, no-aliasing-after-move), the full pre-existing `test_session_builder.cpp` suite (B14–B22, all passing against the same type now), plus every other real/test call site rebuilt and re-run clean. See the ADR's own §4 for the full account. |
| [075](ADR-075-context-budget-fail-closed.md) | When a `ContextProvider` contributor exceeds its own declared `ContextBudget`, should `assemble_context()` silently trim it, or fail closed? | **Judged** (2026-08-23, project-owner sign-off, same-session decision). Closes `docs/planning/2026-08-22-component-role-audit-tracker.md` Finding E (per-contributor budget trimming was invisible — `ContextAssemblyResult.drops` was computed but discarded by every real composite, an I4 gap). `assemble_context()`'s return type widened to `task<result<ContextAssemblyResult>>`; a contributor exceeding its own `max_tokens` now fails the whole call closed (`failure_class::resource`, `"context_assembly.contributor_budget_exceeded"`, naming which contributor by index/name) instead of silently trimming its oldest messages and returning success — matches the already-shipped `token_budget_` precedent (`rt/agent_session.hpp`) at a different layer, rather than inventing a second shape. Deliberately no dual "trim vs. error" config knob: grepped the whole tree first and found zero production callers ever set a nonzero `ContextBudget` today, so there was no shipped graceful-degradation behavior to preserve alongside the cleaner one. `ContextAssemblyResult.drops`/`ContextDrop` are kept, not deleted — real vocabulary `TurnMiddleware::TurnContext` uses independent of this mechanism — but `assemble_context()` itself has no producer of a real one left (confirmed, via `ADR-049`'s own independent finding, that nothing else ever populated one either). `ComposedContextProvider::on_context()` propagates the new failure verbatim. Proven: `tests/test_context_assembly.cpp` (rewritten to prove the failure's class/code/attributed-contributor, plus an exactly-at-budget boundary case), `tests/test_composed_context_provider.cpp` (new Part 1b), signature-only migrations elsewhere (no other caller exercises a nonzero budget). See the ADR's own §3 for why no config knob was added. |
| [076](ADR-076-undeclared-tool-call-leak-refusal.md) | Should the engine notice and refuse, rather than silently pass through, a response that leaks a tool call as raw text when `tool_calling` is declared true but the recovery scan (`scan_response_format_leaks`) isn't armed for that endpoint? | **Judged** (design → red-team → prove complete; approved by project owner, 2026-08-23). Resolves `OpenQuestions.md` OQ-23. New `detect_undeclared_tool_call_leak()` (`core/response_format_leak_scan.hpp`) — detect-only, refuse-not-promote, never touches ADR-023's declassifier — wired into `AgentSession::run_model_call()` at both real insertion points as a structural `else if` sibling to the existing scan branch. An independent, fresh-context red-team pass found one required revision before any code was written (the detector must explicitly skip already-tainted diagnostic content, not merely rely on today's insertion points staying mutually exclusive) and three residuals (a cross-session refusal-amplification surface and a tool-name-enumeration oracle, both host-scope by this project's own locked networking architecture; a domain-specific false-positive risk) — no finding was fatal to the central refuse-not-promote safety claim, which was traced against the real retry/turn-counting code (`failure_class::contract` is retried nowhere in the engine, so a refusal is bounded to exactly one wasted run). 22 new tests, all passing (9 function-level, 13 real `AgentSession::run_model_call()` round trips); full `ctest` 227/229, the only 2 failures confirmed pre-existing via a `git stash` baseline comparison, unrelated to this change. Amends `004-Model-Provider-Plane.md` §2 with the inbound half of "capabilities are declared, not probed." Full record: `docs/planning/oq23-undeclared-tool-call-leak-design-draft.md`. **Renumbered from 074 to 076 (2026-08-23)**: 074/075 were independently claimed by a concurrent session's work (`ADR-074-composed-context-provider-consolidation.md`, `ADR-075-context-budget-fail-closed.md`) that reached `origin/main` first; caught at push time, before this ADR's number appeared anywhere public. |
| [077](ADR-077-agent-executor-capabilityset-bridge.md) | Where does an `executor_kind::agent` workflow node's `CapabilitySet` come from, and how does its runtime bridge to a real `AgentSession` actually work? | **Judged** (2026-08-23, project owner explicitly lifted this file's own prior 2026-08-13 "document only, do not implement yet" instruction this same session, then design → red-team → prove completed against the already-twice-red-teamed design draft). Resolves `OpenQuestions.md` OQ-19. `Executor::capability_ceiling` (declared, static) + `WorkflowSupervisor::initialize()`'s previously-unused `contexts[i]` (the actual granted `CapabilitySet`) + a new `check_workflow_executable(graph, contexts)` overload verifying the grant satisfies the ceiling — never the principal-derived option ADR-070 §7 already steered away from. New `rt/agent_workflow_executor.hpp::agent_session_as_executor_body()`, structurally marked via `AgentExecutorBodyTag`/`std::function::target<T>()` (a `TaggedExecutorBody` wrapper was FATAL as first scoped — breaks every real call site). The concurrent-same-node race the first draft missed (two non-`fan_in` edges converging on one agent-kind node in a round would double-call the same `AgentSession::start_run()` from different threads) is closed by a gather-time quarantine in `WorkflowSupervisor::execute()` routed through the existing failure-policy machinery, not a whole-round abort. Checkpoint/resume amnesia (the first draft's other FATAL finding) is an accepted, TESTED limitation — `restore_from_record()` never reconnects `bodies_`. YAML authoring of the new field is refused loudly, not silently dropped (I6). 26 new tests across two suites (`test_workflow_agent_executor_gate.cpp`, `test_rt_agent_workflow_executor.cpp`), all passing, standalone-built and run (this session's build cache has `AGENTENGINE_WITH_HTTPS=OFF`, which also gates several pre-existing tests out of CTest registration in that tree — not specific to this change; full `ctest` was not re-run). Full record: `docs/planning/agent-as-workflow-executor-design-draft.md`, `OpenQuestions.md`'s own resolved OQ-19 entry. |
| [078](ADR-078-tool-call-hook-stage.md) | Does closing the tool-call gating half of `OpenQuestions.md` OQ-21 (external process hooks) need a new, parallel decision mechanism alongside the already-proven `ApprovalDecider`, or does it extend that seam — and can a hook that reaches an external process be supported without reopening the session-wide blocking hazard 009 §6 already rules out? | **Judged** (2026-08-23, design → red-team → prove complete, run as an 8-agent workflow — 1 design, 3 parallel red-team lenses, 1 judge, 1 core implementation, 1 test implementation, 1 verify — then independently re-verified by the orchestrating session directly). Resolves the minimum, tool-call-point slice of `OpenQuestions.md` OQ-21. New `core/tool_call_hook.hpp` (`ToolCallHookContext` — no `EffectContext&`, no capability type, matching `ModelCallContext`'s I2 discipline — `ToolCallHook`, `hook_call_outcome`, `HookProcessedCall`/`PendingHookDecisionRound`, `HookDispatchAnswer`); `tool_pipeline.hpp` gained `enforce_hook_rewritten_tool_call_provenance()`, mirroring `middleware.hpp`'s `enforce_backend_tool_call_provenance()` exactly; `interaction_reason::hook_decision` added (both directions of the JSON codec, the AG-UI event switch, and `001-Execution-Model.md`'s normative cross-reference all updated). The design pass itself found a real coverage gap before any code was written: inserting the hook stage only right before `invoke_tool()` would leave a hook's rewrite invisible to the suspend-for-approval PRE-check one call site up, silently turning "should suspend for human approval" into "silently denied" — fixed by computing `any_needs_external_dispatch`/`any_needs_approval` from the same post-hook per-call state in one pass. A second fatal finding, closed during implementation (not the original red-team pass): `resolve_hook_decision()` deliberately does NOT reuse `resolve_codeact_ask()`'s `one_shot_approve` shape — an external process's own dispatch answer is a different question from a human's approval, so a `hook_decision` resume re-checks approval need against the real `approval_decider_`/`policy_decider_` and cascades into a genuine approval suspend if one is still needed, never treating the hook's answer as if a human had approved. 46 new tests (`test_rt_agent_session_tool_call_hook.cpp`, H1-H4), all passing; full `ctest` 229/231, the same 2 pre-existing unrelated live-TLS failures already confirmed independent of this session's work, zero new regressions. Run/turn-level gating hooks, a reference external-process dispatcher, and a declarative hook-registration surface remain named, unimplemented follow-on work — `decisions/ADR-067-middleware-turn-point-pre-model-enforcement.md`'s `TurnMiddlewareHook` already covers a real, wired, async, pre-model turn-level GATE for a host that wants one today, documented rather than re-solved here. Full record: `docs/planning/external-process-hooks-design-draft.md` (including its 2026-08-23 Addendum re-grounding every citation against nine days of code drift before this pass started). |
| [079](ADR-079-agent-spawn-runtime-and-capability-minting.md) | Can a model-invocable `agent.spawn` tool construct and drive a brand-new child `rt::AgentSession` to completion from inside the parent's own tool-call loop such that the child's granted `CapabilitySet` can never exceed what the caller holds (I2), both already-proven budget primitives (`trust::SpawnBudget`/ADR-006, `rt::SpawnCostBudget`/ADR-031) fail closed before any child is constructed (I8), and nothing the model's own tool-call arguments supply is ever trusted as authority (I3) — using only already-proven primitives, wiring nothing new into the security-critical path except one reviewable capability-minting function? | **Judged** (2026-08-23, design → four-lens red-team (I2/I3/recursion-concurrency/worktree-isolation) → prove (six pieces, one implementer per piece) → judge, this ADR). Resolves `OpenQuestions.md` OQ-14's "sharpest case" and OQ-16 for a real session. Design B accepted (child = caller's held set attenuated down to the target's declared ceiling via `attenuate()`, `cap::AgentCall` re-rooted per-agent_id to the tighter of live chain depth and the target's own declared depth) — Design A (target's declared ceiling granted unconditionally) and Design C (host-declared "service account," caller needs only a boolean gate) both rejected as I2-equivalent ambient authority, the same class of bug ADR-059 already found and fixed for `invoke_agent_tool()`. New: `rt/agent_spawn.hpp` (`AgentSpawnTool`, `SpawnTargetRegistry`, `SpawnPump` — a single-worker-thread serialization point closing a real, not-hypothetical `AsyncMutex` double-resume hazard and a racy worktree-mint check-then-act, `perform_agent_spawn()`'s nine-step pipeline), `rt/agent_spawn_child_run.hpp` (`run_child_agent_session()`, `derive_on_behalf_of`-derived child identity, background execution unconditionally disabled closing a genuine spawned-child use-after-free), `core/agent_spawn_worktree.hpp` (`mint_spawn_worktree()` — the critical I2-1 fix: a `branch`-mode grant is intersected with what the caller already holds on its own mount, never uncapped over the whole branched tree), `trust/agent_spawn_capability.hpp` (`mint_child_spawn_capabilities()`, Design B's central function). OQ-16: `AgentSession::set_static_instructions()` plus `core/session_builder.hpp` wiring `trust::push_side_summary()` — its first real caller — into both the parent and spawned-child instructions. Independently re-verified by the judging pass, not taken on self-report: full `ctest` 205/205 passing (2 pre-existing unrelated skips), the four new test binaries re-run directly (136 checks total, all passing), all four re-run clean under MSVC ASan. **Two of the design's own falsifiable claims (C3, C9 — the `SpawnPump`'s concurrency safety) are judged INCONCLUSIVE, not CORRECT**: the mechanism is sound by code-level construction (only its own worker thread ever resumes the `consume()` coroutine or touches the worktree ref-store), but no test exercises real multi-OS-thread concurrent `submit()` calls, and TSan was not run at all (no supported `-fsanitize=thread` in this MSVC/Windows toolchain). C8's own adversarial negative control (a real `Backgroundable` tool reproducing a use-after-free with the fix removed) was likewise never built — the shipped guard is structurally sound by inspection, but this judging pass corrects the prove-phase report's claim of an ASan/TSan-verified repro to INCONCLUSIVE. Other named residuals: no ref-store GC/reclamation, no refund on ordinary child-run failure (mitigated by a new per-principal `SpawnQuota`, not eliminated, both inherited from ADR-031's own Judged no-refund design), no merge-on-completion for a branch-mode child, `SpawnWorktreeGrant`'s one-grant-per-axis shape fails closed rather than guessing when a caller holds more than one distinct `FsRead`/`FsWrite` on its own mount, and the `AGENTENGINE_WITH_HTTPS`-gated half of the `session_builder.hpp` change was reviewed but not build-verified in this pass. Full record: `docs/planning/agent-spawn-runtime-design-draft.md`. |
| [080](ADR-080-sandbox-backend-registry.md) | Does `Strict` sandbox-profile resolution get a real, host-curated backend registry, or stay a permanent no-op? | **Proposed** (design → red-team → prove phases complete; awaiting project-owner "Judged" sign-off, matching ADR-070/071/072's own flow). Closes the gap `decisions/ADR-012-sandbox-profile-template-parameter-kind.md` §9 named as deferred, not decided against: `SandboxBackendRegistry` (`sandbox/sandbox_backend_registry.hpp`, new) mirrors `ChatClientRegistry`/`ToolRegistry` (host-curated, never auto-discovered) — `register_backend<B>()`, `resolve_strict(platform_id)` (008 §3's rule against the real registered set, `strict_eligibility{eligible, named_only}` bounding the blast radius of registering a backend for one agent's named selection only), and `resolve_named(HostSandboxSelection)` (the actual "always use backend X" config surface `Strict` alone cannot express, `HostSandboxSelection`'s `explicit` constructor raising the bar against an accidental I3 violation without claiming to prove provenance). `register_agent<A>()` grows an additive, defaulted `SandboxBackendRegistry const*` parameter — every existing call site unaffected. A real, independent 3-agent red-team pass (security/I2-I3, C++ correctness, architecture-fit) found and fixed a blocking bug before any code was written: the original sketch closed over a fresh, default-constructed backend per call, silently discarding per-instance state between `create()` and the next `exec()`/`destroy()` (traced by one reviewer to a spawned process being killed on the same call's return via `JobObjectLimits`'s destructor) — fixed by one long-lived `shared_ptr<B>` per registration. 15 new tests (`test_sandbox_backend_registry.cpp`, `test_agent_registry_sandbox_backend_registry.cpp`), the load-bearing one a literal regression test for that confirmed bug; full `ALL_BUILD` clean, full `ctest` 213/223 passing (the only 10 non-passing pre-existing and confirmed unrelated — gated behind an embedded-CPython toolchain check this environment lacks). Amends `008-Sandbox-and-Isolation.md` §3. Explicitly out of scope: the microVM backend itself (a separate ADR, per project-owner direction) and real production consumption of a resolved backend — nothing in this engine constructs a `SandboxHandle` from one outside tests, named as a tracked residual, not silently dropped. **Renumbered from 079 to 080 (2026-08-23)**: 079 was independently claimed by a concurrent session's work (`ADR-079-agent-spawn-runtime-and-capability-minting.md`) that reached `origin/main` first. Full record: `docs/planning/sandbox-backend-registry-design-draft.md`. |
| [081](ADR-081-jailed-python-worker-process-slice-1.md) | Does routing `execute_code` through a real, separately-jailed worker process close the gap between what 008 §1b/§3 and 010 §6 require for T3 code (kernel jail as a mandatory third layer, on top of interpreter mediation) and what the embedded CPython interpreter actually ran with (in-process with the host, zero OS-level containment) — without reopening I2/I3 or regressing the existing native-jail Windows suite? | **Judged** (2026-08-23; formalizes work already implemented in commit `a60fc3d` under project-owner direction — design → red-team → prove already complete there, this ADR independently re-built and re-ran the evidence rather than taking the commit message on self-report). Closes `docs/planning/2026-08-22-component-role-audit-tracker.md` Findings Q ("zero OS-level resource containment on the interpreter process") and R ("the RFC's mandatory kernel-jail backstop is entirely absent, and a same-day code comment had asserted this was permanently resolved without any ADR actually deciding that") — **008 §1b/§3's "kernel jail, permanently, for the interpreter" requirement is reaffirmed, not amended**; this ADR is the real work that requirement was calling for. `NativeJailBackend::create_python_worker()`/`exec_session()` (`src/backends/native_jail/native_jail_backend.{hpp,cpp}`) spawn an AppContainer + Job-Object-jailed `agentengine_python_worker.exe`, additive to and never touching the existing per-exec `create()`/`exec()`/`destroy()` path; a session-scoped watchdog thread started before `ResumeThread()` gives a real, externally-preemptible wall-clock kill and a real Job Object memory cap, re-measured directly for this ADR (an 800ms-budget busy-loop killed in 804ms; a 512MiB allocation under a 64MiB cap never completes as `ALLOCATED_OK`). `call_tool`/`agent.tools` relay through the real host-side `bridge_tool_call()` with host-minted `exec_seq` anti-replay (`RT1 Finding 1`: a mismatched-`exec_seq` `worker_query` is never dispatched, the worker is terminated) and single-flight `call_mutex` (`RT1 Finding 2`); a background idle-phase CPU budget (`RT2 Finding 1`) and watchdog-before-resume (`RT2 Finding 4`) close the resource/DoS/lifecycle lens's own findings, both re-verified at their fix sites for this ADR, not merely cited. `mediated_python_runner.cpp` is now a confirmed thin IPC client (1561→97 lines, no `Python.h`). Re-run for this ADR: the design's own 3 positive controls all pass; the pre-existing 10-test native-jail/tool-relay/B7-fix/containment-invariance suite is 100% green (zero regression); the disclosed Slice 2 regression (6 test files depending on unbuilt file/socket relay) was independently reproduced and confirmed to be clean, named denials, never a crash or hang. **Honestly disclosed, not-eliminated residuals**: Slice 2 (real file/listdir/socket relay) is unbuilt, so `execute_code` sessions needing file or network I/O do not yet work under this model at all; Linux (`LinuxNativeJailBackend`) parity for the jailed-worker model does not exist; `execute_shell`'s own process-spawn model (tracker Finding O) remains unreconciled with this design; `cpu_ms_cap` inherits ADR-004's own best-effort-only finding (`wall_ms` is the dependable bound); ADR-041's accepted Windows ACE-leak residual is unchanged. **Provenance gap named on the record**: the original design phase's two competing candidates were compared inside an agent workflow whose transcript was not preserved as a repository artifact, so this ADR reconstructs its red-team record (§5) from labeled findings (`RT1`/`RT2`) actually embedded as comments at their fix sites plus the tracker's own prose, rather than fabricating a steelman for a rejected design nobody can now check — flagged as process guidance for future security-critical passes run the same way. **Renumbered from 080 to 081 (2026-08-23)**: 080 was independently claimed by a concurrent session's work (`ADR-080-sandbox-backend-registry.md`) that reached `origin/main` first. |
| [082](ADR-082-native-jail-promotion-gate-008-9.md) | Does the `native-jail` promotion gate (008 §9, G1-G8) actually hold for the real, shipped M2 Phase C implementation, gate by gate, on the evidence that exists today — and where it doesn't, what specifically is missing? | **Judged** (2026-08-23) — a synthesis-and-verification pass over already-shipped M2 Phase C work (tasks C1-C6, all marked "(done)" in `docs/planning/milestone-2-tools-capabilities-sandbox-breakdown.md`), not a fresh design decision: no ADR had ever formalized this isolation boundary as a whole, only ADR-004 (Spiked at the time, superseded implementation-wise, since promoted to Judged 2026-08-23 by this same-day pass) and ADR-041 (Judged, one narrow residual). Re-ran the Windows native-jail suite directly for this ADR (`runner_stubs`/`backend_windows`/`abuse_corpus_windows`/`parity_windows`/`ambient_authority_windows`/`teardown_cycles_windows`, 6/6 passing) and read every Linux counterpart in full rather than trusting the roadmap's own "(done)" language. **G1 (parity): Judged**, correctly scoped — `tests/helpers/abuse_case_corpus.hpp` is one shared table compiled into both platforms' binaries, structurally proving parity for 5 outcome-classifiable cases; fork-bomb and fs-escape are deliberately excluded from that claim (asymmetric semantics), not silently folded in. **G2 (containment): Judged on Windows in full** (including fs-escape, ADR-041's own Case 4); **Judged on Linux minus fs-escape** — the Linux abuse-corpus test's own header states plainly `CLONE_NEWNS` gives a private mount namespace with nothing populating a restricted view, a real, already-tracked gap (GitHub issue #5), not discovered here. **G3 (no ambient authority): Judged on Windows** (all 4 axes, real canary positive controls); **Judged on Linux minus filesystem/process visibility** — traced to the identical root cause as G2's Linux gap (no `/proc` remount either), one defect not two, already the explicit subject of `docs/planning/linux-native-jail-pivot-root-containment-design-draft.md`. **G4 (teardown): Judged on Windows** at the bounded, disclosed 300-cycle/no-ASan scope (a real sensitivity margin, not just speed — checked directly); Linux file exists but unverified this pass (no Linux environment). **G5 (cold start)**: correctly out of scope pending 023's M8 baseline, not a gap. **G6 (downgrade visibility): open** — deliberately, honestly deferred by M2 Phase C's own tracker, still unclosed, no follow-on tracked since. **G7 (session boundary): open, INCONCLUSIVE** — unlike G5/G6, absent from M2 Phase C's own deferred list entirely (a tracking gap, not just an implementation one); plausible by code-level construction (`create()`/`create_python_worker()` mint a fresh `Instance` every call, no pooling code path exists to reuse one across sessions) but never proven by an actual canary/pooling test — the same "sound by construction, not CORRECT" call ADR-079 made for its own unproven concurrency claim. **G8**: not applicable to `native-jail` by 008's own text (wasm-only). No code changed in this pass. **Residuals named**: the pivot-root design draft closes G2+G3's shared Linux gap in one fix if built; G6 needs a real negative test plus diagnostics/trace/metrics wiring; G7 needs a real canary test turning today's structural argument into a proven fact before anyone adds instance pooling and quietly invalidates that argument's own premise; G4's Linux claim needs re-verification the next time a Linux build environment exists. **Update (2026-08-23, same day):** G6 and G7 closed with real code + tests, not left open. G6: `SandboxBackendResolutionEvent::downgraded` (new field, `sandbox_backend_registry.hpp`) distinguishes a real fallback from an ordinary win — `test_sandbox_backend_registry.cpp` items 8a-8c, paired with the registry's pre-existing "no fallback → startup fails" negative test (item 5). G7: `tests/test_native_jail_session_boundary_windows.cpp` (new) proves a canary planted in one jailed-worker session is unreachable from a second, concurrently-live session — upgraded from the original pass's INCONCLUSIVE (code-level argument only) to CORRECT (real evidence against the kernel-enforced OS-process boundary ADR-081 gave this claim). Only G2/G3's shared Linux root cause remains open from the original pass. **Update (2026-08-23, same day): G2/G3's shared Linux root cause is also closed** — see `decisions/ADR-083-linux-native-jail-pivot-root-containment.md`, run against a real Linux environment (WSL2) obtained specifically for this closure. Nothing from this pass's original findings remains open except G4's Linux re-verification (still needs a Linux environment run, now available but not yet spent on that specific re-check) and G5 (correctly out of scope pending 023's M8 baseline). |
| [083](ADR-083-linux-native-jail-pivot-root-containment.md) | Does a real `pivot_root` + bind-mount jail close `LinuxNativeJailBackend`'s filesystem and process-visibility gap (008 §9 G2/G3's Linux half) without reopening I2/I3 or regressing the existing Linux native-jail suite? | **Judged** (2026-08-23, same day as ADR-082's G6/G7 closures). Design + self-red-team phase already complete before this ADR: `docs/planning/linux-native-jail-pivot-root-containment-design-draft.md` (MS_PRIVATE-first ordering to avoid a host mount-table leak; a dedicated tmpfs jail root, not the cgroup directory; the required second `MS_REMOUNT|MS_RDONLY` call for a read-only grant, since the initial `MS_BIND` call silently ignores that flag alone; `pivot_root` not `chroot`; a fresh, namespace-local `/proc`). Prove phase: a real Linux 6.6 kernel (WSL2/Ubuntu) obtained specifically for this ADR — cgroups v2 fully mounted, real seccomp-BPF, working namespace creation under root. `LinuxNativeJailBackend::setup_jail()` (new) implements the design verbatim inside the child, before `install_seccomp_filter()` (`mount`/`umount2`/`pivot_root` are all three in that filter's own denylist). Measured, not merely argued: an ungranted host path is unreadable (`READ_DENIED err=2`) while a granted one is readable (`READ_OK`); a read-only-granted mount rejects a write with `EROFS` while a read-write one accepts it, host-side-content-verified both ways; host `/proc/mounts` line count is identical (41→41) before and after hundreds of jail creations, directly confirming the design's MUST-FIX 1 concern never fires; a new 4th teardown-census axis (`jail_dirs`) shows zero growth across 300 cycles. A real pre-existing bug was surfaced running the full suite under `ctest -j 4`: every Linux native-jail test shares the same default host paths and a process-local (not host-unique) id counter, so concurrent test binaries could mint the identical id and collide — fixed with `RUN_SERIAL TRUE` on all six gated tests, the same treatment `test_native_jail_teardown_cycles_windows` already has. Full suite 100% green both before (155/155) and after (161/161, the 6 new/gated Linux tests included) this ADR's changes. Every existing Linux native-jail test's `SandboxSpec` needed an explicit toolchain grant (`tests/helpers/native_jail_linux_toolchain_mounts.hpp`, new) for `/bin/sh` itself to be reachable inside the new jail — the exact "no implicit mount" consequence the design predicted. **Update (2026-08-23, same day): the id-collision bug is fixed at its root**, not just worked around — `create()`'s id is now PID-prefixed (`getpid()` + counter, was counter-only/process-local), verified by launching two copies of a gated test concurrently outside `ctest`/`RUN_SERIAL` entirely, both passing. `RUN_SERIAL` stays as defense in depth. Same update also closed ADR-082's G4 Linux residual (re-ran `test_native_jail_teardown_cycles_linux.cpp` directly: `jail_dirs 0 -> 0` across 300 cycles) — see ADR-082's own row. **Remaining residuals**: the test-only toolchain-mount helper grants host paths at identical guest paths, acceptable for this M2 raw-shell-exec test scope but explicitly not a production filesystem-visibility policy (010's interpreter-level mediation is that boundary); Linux parity for the *jailed Python worker process* (ADR-081, Windows-only) remains a separate, still-open gap this ADR does not touch. |
| [084](ADR-084-kata-backend-slice-1.md) | Now that `native-jail`'s own 008 §9 gate has reached Judged on both platforms (ADR-004, ADR-083), does a first-party `KataBackend` (Design A, narrowed per the microVM reversal design draft's §7 C2 finding) work as a real `SandboxBackend`, and can it clear an independent red-team pass before being treated as more than a spike? | **Judged** (2026-08-23) — real, new code, not a formalization. Operates a project-owner-directed, narrowed exception to 008 §1's locked "no `microvm` profile" decision (the decision itself is not amended — see `docs/planning/microvm-first-party-backend-design-draft.md` §9, which recorded this same day that reopen condition (i), native-jail's own gate reaching Judged on both platforms, was met). Scope: ONE pinned Kata Containers release (4.1.0, no upstream LTS track exists — checked directly), ONE runtime class (`kata-clh`/cloud-hypervisor, chosen over `kata-qemu` specifically to bound the CVE-tracking surface per the design draft's own C2 finding), registered only via `register_hardware_isolation_backend()` (ADR-080, structurally `named_only`, no call site registers it yet). `src/backends/kata/kata_backend.{hpp,cpp}` shells out to `ctr` (no new C++ dependency vendored) via `posix_spawn`; real end-to-end evidence against a live deployment (a dedicated WSL2 Ubuntu-24.04 distro, deliberately separate from the native-jail Linux work's own distro after a real conflict risk — a root-cgroup-migration script — was raised and avoided): a real guest kernel distinct from the host, state written by one `exec()` call read back correctly by a later one on the same instance (the real-VM analogue of `test_sandbox_backend_registry.cpp` item 1's own regression test), correct exit-code classification, closed-handle fail-closed, idempotent `destroy()`. **A real, load-bearing infrastructure bug found and fixed during bring-up** (not in this backend's own code): the shipped Kata `runtime-rs/configuration.toml` default symlink pointed at QEMU, not cloud-hypervisor — every `ctr run` silently launched the wrong VMM until this was caught by directly inspecting the running process tree, not by trusting configuration intent. **A real independent red-team pass** (fresh reviewer, no prior context, the same bar ADR-004 §12 met earlier the same day) against the real shipped code found and this same pass fixed two BLOCKING findings, both verified by execution: an unbounded output-drain (a 200MB guest `exec()` landed in full in host RSS with no cap — fixed with a 16 MiB cap matching `LinuxNativeJailBackend::drain_pipe_bounded()`'s own constant) and an unbounded subprocess wait (a hung/slow `ctr` call, including inside `destroy()`'s own cleanup sequence, had no timeout at all — fixed with a 30s deadline, `SIGKILL`-on-expiry, and a new correct use of the existing `exec_outcome_class::timeout` enum value); plus two MINOR fixes (a `pipe2()` partial-failure fd leak; `destroy()`'s previously fully-silent cleanup failures now logged) and one MINOR finding deliberately left unfixed (PATH-resolved `ctr` binary, no demonstrated exploit). **Honestly disclosed, not-eliminated residuals**: `SandboxSpec::capabilities`/`mounts`/`limits`/`net` are NOT enforced by this backend at all (Kata's own defaults — no CNI, ephemeral per-image rootfs — are the only containment that exists today); no GPU passthrough (would need a new `capability_kind`, deliberately not added); no upstream-published checksum manifest exists for the Kata release tarball to cross-verify this project's own recorded SHA-256 against (a narrower verification bar than ADR-013's mbedTLS precedent, named rather than glossed over); a real accuracy nuance found during the fix-verification pass itself, where a correctly-capped-but-slow call can be labeled `timeout` rather than `ok` under one specific interaction; no 008 §9 G1-G8 promotion-gate evidence exists for this backend yet. |
| [085](ADR-085-jailed-python-worker-slice-2-handle-relay.md) | Does `HandleRelay` give the jailed Python worker (ADR-081 Slice 1) real, capability-mediated file and socket I/O, closing Slice 1's own named residual, without reopening I2/I3 or regressing the existing suite? | **Judged** (2026-08-23). Real, new code: `dispatch_open`/`dispatch_listdir`/`dispatch_file_read`/`dispatch_file_write`/`dispatch_file_close`/`dispatch_connect_authorize`/`dispatch_connect_send`/`dispatch_connect_recv`/`dispatch_connect_close` (`native_jail_backend.cpp`), the worker-side `_AeRelayFile` class and socket relay wrappers (`python_worker_mediation.cpp`'s bootstrap source). **Two real implementation-time falsifications, each found by running the actual tests, not by review, and disclosed rather than hidden**: (1) the ORIGINAL design planned `DuplicateHandle`-based file relay; real I/O against the duplicate fails with `ERROR_INVALID_HANDLE` inside the AppContainer'd worker even though the duplicate is confirmed open (`GetHandleInformation` succeeds) — a real Windows AppContainer limitation, not a wiring bug; shipped design keeps the real `HANDLE` host-side and relays read/write/close by `file_id` instead, the same shape sockets already needed. (2) the socket_id<->socket correlation was planned as an ordinary instance attribute; this vendored CPython's `socket.socket` carries no `__dict__` at all — fixed with a `weakref.WeakKeyDictionary`. **A real independent red-team pass** (fresh reviewer, no prior context) found and this same pass fixed one BLOCKING finding, verified by a new positive control: only `connect`/`send`/`sendall`/`recv`/`close` were mediated on `socket.socket` — `bind`/`listen`/`accept`/`connect_ex`/`sendto`/`recvfrom`/`makefile`/`dup`/`detach`/`share`/`shutdown` were still the real, unmediated CPython implementation with zero capability check (ADR-004 AC-S1's own measured evidence covers `connect()` specifically, not this wider surface) — fixed with explicit denial, the same defense-in-depth posture the existing `os.*` denial list already uses; plus one real bug (a `uint16_t` cast on an unvalidated guest-supplied port was undefined behavior, reachable via an ordinary typo, not an attack) and three MINOR fixes (a base64-decode-before-size-clamp gap that undercut the stated per-call memory ceiling by ~64x; a comment overstating the outer watchdog as a backstop for a host-thread-blocking call it cannot actually interrupt; an exception-unsafe test-only resolver-override seam, now RAII-guarded). **Executed evidence**: 5 of ADR-081's own 6 disclosed-failing tests now fully pass; the 6th (`test_reference_agent_task_corpus`'s pandas_groupby) still fails on a confirmed pre-existing, unrelated `extra_sys_path` ACL gap (verified via direct `git stash` bisection against the unmodified baseline); two new positive/negative controls (a real socket round trip under a real `cap::NetOut` grant, requiring a new SSRF-bypass-free test-only resolver-override seam since `resolve_and_validate` categorically blocks every hermetic same-machine target by design; kernel-level, not merely Python-wrapper-level, read-only enforcement) both pass; full project suite 264/265 passing both before and after the red-team fixes, zero collateral regression. **Honestly disclosed, not-eliminated residuals**: the pandas-import ACL gap; no `ResourceLimits::net_bytes` reconciliation for the raw-socket relay's flat per-call byte cap; no UDP relay (denied outright) or IPv6; a non-ASCII UTF-8 chunk-boundary decode gap in size-bounded text reads (unexercised by any current ASCII fixture); `execute_shell`'s still-separate process-spawn model (unchanged from ADR-081); Linux parity (still unbuilt); the test-only resolver override's "never used from production" is convention-enforced only, not type-system- or build-flag-enforced. |
| [086](ADR-086-kata-backend-slice-2-enforcement.md) | Does `KataBackend` Slice 2 (ADR-084's own named residual: enforcing `SandboxSpec::mounts`/`limits`/`net` instead of silently ignoring them) close that gap for real, and can it clear an independent red-team pass before being treated as more than a spike? | **Judged** (2026-08-24). `MountSpec` grants become real `ctr run --mount type=bind,...` bind mounts (`BlobRef` still fails closed, same check `LinuxNativeJailBackend::create()` has); `ResourceLimits.memory_bytes` maps to `--memory-limit`, verified REAL — a 256 MiB cap produced a guest showing ~257 MiB via `free -m`, not the ~2GiB unconfigured default; `wall_ms`/`output_bytes`, if set, override `run_ctr()`'s fixed defaults per already-created instance (verified: a 3000ms cap cut a guest `sleep 10` off at ~3.02s); `NetPolicy` now fails closed on anything beyond the default `deny_all` (no CNI/egress-proxy exists to honor a real allowlist). `cpu_ms`/`pids`/`fds`/`disk_bytes`/`net_bytes`/`quota_bytes`/`capabilities` remain unenforced, each named with its own reason. **A real independent red-team pass** (fresh reviewer, no prior context, the same bar ADR-004 §12/ADR-084 §4 met) found and this same pass fixed one BLOCKING finding, verified by execution: `create()` built the `--mount` value by direct string concatenation into a SINGLE comma-delimited `ctr` value — a `guest_path` (or `source`) containing an embedded `,src=...,dst=...` segment silently hijacked the mount to an ARBITRARY host path (reproduced: bind-mounted the host's real `/etc` into the guest, readable via `cat /mnt/hijacked/passwd`), a full I2 violation through a spec field this backend claims to enforce — fixed by rejecting a comma in either path outright (`ctr`'s mount grammar has no escaping mechanism). Also fixed a REAL GAP (a failed `create()` — reproduced via a nonsensically tiny `memory_bytes` — left a containerd container object leaked with no handle ever reaching the caller, unreachable by any later `destroy()` call; fixed with best-effort cleanup on the failure path, mirroring `destroy()`'s own posture) and documented a scope boundary as a MINOR/judgment-call finding (`wall_ms` does not and should not bound `create()`'s own VM-boot call — stated explicitly in the header comment now rather than left for a reader to infer). Re-verified: both `test_kata_backend_linux` (Slice 1) and the new `test_kata_backend_slice2_linux` 100% passing after the fixes, zero regression; the exact injection payload now returns `kata_backend.mount_path_invalid`; the leak repro's `ctr containers ls` count is unchanged before/after the fix. |
| [087](ADR-087-sandbox-spec-capability-enforcement.md) | Does `SandboxSpec::capabilities` (ADR-086 §5.1's own named residual) become the real authority behind `mounts`/`net` for every mount/net-shaped `SandboxBackend` — not `KataBackend` alone, since that would itself create a per-backend divergence in what `SandboxSpec` fields mean — and can the design clear an independent red-team pass before any implementation lands? | **Proposed** (2026-08-24; design → red-team → fix → implement → local Windows verification complete, awaiting project-owner Judged sign-off). Two new capability kinds, `cap::SandboxMount`/`cap::SandboxNetOut` (`trust/capability.hpp`) — deliberately not a reuse of `cap::FsRead`/`FsWrite`/`NetOut` (those are `mount_id`-keyed, Worktree/`FileSystemAdapter`-mediated; `MountSpec` names a raw host path directly — conflating the two would be a real capability-confusion hazard, the same judgment ADR-071 already made for `NativeExec` vs. `Exec`). A single free function, `authorize_spec()` (`sandbox/sandbox.hpp`), called first-thing by all four real mount-granting entry points: `KataBackend::create()`, `LinuxNativeJailBackend::create()`, `NativeJailBackend::create()` (Windows), and `NativeJailBackend::create_python_worker()` (Windows — the mediated Python worker's own separate entry point). Opt-in, scoped to presence of the relevant grant kind specifically (not `CapabilitySet::size()`) — every existing call site holds zero grants of either kind today, so behavior is byte-for-byte unchanged for all of them. **An independent adversarial red-team pass run against the pre-implementation DESIGN DRAFT, before any code existed** (per CLAUDE.md's `design → red-team → prove → judge` for an I2-touching, three-backend change) found this design unsafe to implement as originally written: FATAL — `path_prefix_covers()` reuse has no defense against a literal `..` lexically defeating prefix coverage with zero filesystem access, fixed by rejecting any `.`/`..` path component outright, the same "reject the injection surface entirely" move `KataBackend`'s own comma-injection fix (ADR-086) already established; BLOCKING — the mechanism as drafted never engaged on `NativeJailBackend::create_python_worker()`, the actual production mount-granting path for the mediated Python interpreter (a structurally separate entry point from `create()`), fixed by enumerating and wiring every real entry point, not just the three-method concept surface; BLOCKING — the drafted opt-out (`spec.capabilities.size() == 0`) would fail OPEN the moment any unrelated capability (e.g. `cap::Background`) appeared on a spec with real mounts, fixed by scoping the opt-in to presence of the SPECIFIC relevant grant kind instead; BLOCKING — `NetPolicy` enforcement would have stayed divergent across backends even after this design shipped (Kata already fails closed on non-`deny_all`, native-jail silently ignored it), fixed by giving both native-jail backends the identical unconditional check Kata already had; plus three MUST-FIX findings (an ambiguous, type-mismatched `quota_bytes` "0 = unspecified" rule borrowed from a field shape it didn't match — fixed by not carrying `quota_bytes` on the new capability at all until quota enforcement itself exists; an unpinned `host_allowlist` matching grammar — fixed, pinned to exact match after lowercasing, no wildcard support; a missing I3 struct comment — fixed, matching the `NativeExec` precedent). All fixed before implementation began, not after. **Verified this session** (Windows-only, no local Linux/Kata environment available): full local rebuild, zero new errors/warnings; new pure-logic suite `test_sandbox_capability_authorization.cpp` (16/16 checks, platform-independent, runs in CI everywhere) proves the fixed coverage/opt-in/`..`-rejection/case-insensitive-net-matching/multi-grant/`BlobRef`-passthrough behavior directly; full local `ctest` run confirmed zero regression on every existing Windows-buildable test. **Honestly disclosed residuals**: symlink-based escape inside an otherwise-correctly-granted directory is not detected (same unsolved class of gap ADR-071 §6 item 3 already names for a different surface — a real fix needs a handle-anchored check at the point of use, separate work); `cap::SandboxMount::host_path_prefix` is an unbounded raw filesystem prefix with no ceiling (a syntactically valid `"/"` grant authorizes everything, an operator-discipline risk, disclosed not fixed); Linux (`LinuxNativeJailBackend`, `KataBackend`) and the Windows-only `create_python_worker()` integration path were wired and compile-reviewed but not independently re-verified end-to-end against a live Linux/Kata run this session (no such environment available) — a future session should confirm the existing Linux native-jail/Kata suites still pass unmodified (expected, since they hold zero `SandboxMount`/`SandboxNetOut` grants today, but not yet independently proven). |
| [088](ADR-088-kata-backend-abuse-corpus.md) | Does `KataBackend` get its first 008 §9 G1/G2 abuse-corpus promotion-gate evidence (the last named residual in `kata_backend.hpp`'s own header comment), and can `exec_outcome_class::oom` be closed via an exit-code-137 heuristic mirroring `LinuxNativeJailBackend`'s own fallback? | **Proposed** (2026-08-24; design → red-team → fix → implement → local verification complete, awaiting project-owner Judged sign-off). The OOM-classification design was **investigated and REJECTED**, not shipped: an independent red-team pass found the exit-code-137 analogy to native-jail's own heuristic a non sequitur — native-jail's version rests on a documented Linux kernel `wait()` fact about a process it directly reaps as PID 1 of its own namespace, while `KataBackend`'s `outcome.exit_code` reflects the HOST-side `ctr` CLI's own exit, three RPC hops removed from the guest's actual death, with no source anywhere in this repo confirming any hop preserves the 128+signal convention, no fallback for the direct-SIGKILL-to-`ctr` case (`exit_code` stays -1, never 137), and no documented default guest-VM memory size to size a positive control against (the "~2GiB default" the original design assumed was itself unsourced). `exec_outcome_class::oom` stays unreachable for this backend — a named, investigated gap now, not a silently-unattempted one. The SAME red-team pass, reading `exec()`'s timeout path closely enough to evaluate that design, surfaced a real, separate, previously-undisclosed BLOCKING gap: `run_ctr()`'s `wall_ms`-timeout `kill(pid, SIGKILL)` only terminates the HOST-side `ctr` CLI wrapper, not the guest-side process it was attached to, which could keep running orphaned inside the persistent container until a later call happened to reap it — `exec_outcome_class::timeout` was being returned without the workload having actually stopped. **Fixed this ADR**: `exec()`'s timeout branch now also issues a best-effort `ctr tasks kill --exec-id <id> --signal SIGKILL <container>` against the same `--exec-id` the timed-out call minted, targeting the guest process directly; failure logs to stderr rather than silently vanishing, matching `destroy()`'s own established cleanup posture. New test `tests/test_kata_backend_abuse_corpus_linux.cpp`: infinite-loop/`wall_ms` (contained, with a heartbeat-file positive control that specifically proves the guest process actually stops — not merely that the host CLI call returned — the exact defect this ADR fixes) and unbounded-output/`output_bytes` (captured-bytes-only claim, same shape as native-jail's own corpus) each with a positive control per 008 §9 G2; fork-bomb/`pids` is a deliberately bounded, non-asserting documentation case (no `pids` mechanism exists for this backend at all — silently omitting the case would make the corpus non-comparable to native-jail's four-case shape without saying so, but a literal unbounded bomb against an uncapped guest is a real machine-safety risk this project's own "hostile tests are resource-capped" rule forbids). **Verified this session**: the pre-implementation design went through an independent adversarial red-team pass (resumed once after an interruption) before any code was written; this repo's already-configured `build-linux` WSL/Linux CMake cache was reconfigured with `-DAGENTENGINE_BUILD_KATA_BACKEND=ON -DAGENTENGINE_KATA_SANDBOX_TESTS=ON` (confirmed landed via `CMakeCache.txt`, not assumed) and all three Kata test targets plus the `kata_backend` library compiled clean against a real Linux g++ toolchain — more verification than any prior Kata slice received in-session, though still short of execution against a live deployment; full local Windows `ctest` run, 247/247 passing, zero regressions. **Honestly disclosed residuals**: no real Kata/containerd deployment was reachable anywhere in this session, so the new test remains un-executed (same disclosed limitation every prior Kata test in this tree already carries); the unbounded-output case's own guest-process-orphan risk is named but not fixed (scope-bounded to the timeout path the red-team actually found concretely broken); the `ctr tasks kill --exec-id` flag syntax this ADR's fix depends on is not independently re-verified against a live deployment, carried forward from every other `ctr` CLI assumption already in this file; `ResourceLimits::pids`/`fds`/`disk_bytes`/`net_bytes` remain unenforced (Slice 2's own unchanged gap). |
