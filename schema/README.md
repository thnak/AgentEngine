# `schema/`

JSON Schema for the declarative agent/workflow format (YAML/JSON documents that compile to the
same metadata as the C++ CRTP form, per invariant I6), plus protocol fixtures used by the
conformance suites. Governing RFC: **015-Declarative-Agent-Format.md**.
