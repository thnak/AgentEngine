# `plugins/`

First-party plugin sources that build to `.wasm` components against the `wit/` worlds — including
the C/C++ library track (document extraction, archive handling, media codecs, tokenization, and
CLI-style tools like `git`/`ripgrep` wrapped for `ShellRunner` dispatch). Governing RFC:
**009-Plugin-and-Extension-System.md**, especially §7.
