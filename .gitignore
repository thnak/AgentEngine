# Build
build/
build-*/
out/
cmake-build-*/
CMakeUserPresets.json
compile_commands.json
.cache/

# Artifacts
*.o
*.obj
*.a
*.lib
*.so
*.dylib
*.dll
*.exe
*.pdb
*.ilk
*.exp

# WASM plugin build output
*.wasm
*.aepkg
wit-bindgen-out/
target/
# cargo-component regenerates this from wit/ on every build -- never hand-edited, never committed.
tests/fixtures/**/src/bindings.rs

# Python (interpreter fixtures, tooling venvs)
__pycache__/
*.py[cod]
.venv/
venv/

# Local runtime state
*.log
recordings/
checkpoints/
workspaces/
.aengine/

# CodeGraph local index -- regenerated by `codegraph init`, never committed
.codegraph/

# Secrets — never commit
.env
.env.*
*.pem
*.key
secrets.toml

# Editors / OS
.vs/
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# Local-only API credentials for Milestone 3 Phase H reference-agent fixture recording -- never commit
api-test.txt

# Local-only provider credentials for the `live-network` ctest label
# (tools/run-live-provider-tests.ps1 reads this) -- never commit
apt-openrouter.txt

# Local debug dumps of live-provider chat transcripts -- scratch, never committed
response-dumps/
dumps/
